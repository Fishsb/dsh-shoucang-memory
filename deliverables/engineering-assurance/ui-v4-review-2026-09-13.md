# 守藏面板 UI 方案 v4 · 整体审查（2026-09-13）

> **审查对象**：`deliverables/ui-redesign-v4-2026-09-12.html`（含 ①–⑬ 施工图纸与自审附录），旁证 `ui-redesign-proposal`(v1) / `ui-redesign-v2` / `ui-redesign-v3` / `ui-plans-index`(方案 A/B/C) / `ui-after` / `ui-preview`。
> **性质**：**只读审查，未改任何代码**（`git status` 全程 `client.js` == HEAD `9f6c920`）。
> **方法**：原型全文（去标签后 21,009 字）逐节抽断言 → 回查 `client.js`(3502 行，node 口径) / `src/panel*.ts`(6 模块) / `skill/engine/criteria{,-gate}.json` / 门禁实测。
> **结论（一句话）**：**设计方向（首屏换血 + 两处长页 Tab 化）值得落地；但方案文档的「证据层」不可作为落地依据**——⑦ 查漏补缺 7 项中 5 项与现树矛盾，照单执行会产生重复建设。

---

## 一、可信度分层：v4 每条断言的实测核验

### ✅ 正确且高价值（直接采信）

| 断言 | 实测证据 |
|---|---|
| 7 个响应式断点（@media） | `client.js` L612/617/623/634/661/669/676 = **7 处**，范围 L612–681，与原文一致 |
| `Cfg.DEF.navWidth = 200` 与 `applyNavWidth()` 兜底 `216` 不一致 | L777 = `navWidth: 200`；L3172 = `get('navWidth', 216)` → **真缺陷，原文正确** |
| 默认首屏是配置原文 | L3090 `var currentView = 'file'` → 打开面板先看 YAML，**原文正确** |
| 图标 5 个供 8 视图复用 | `ICONS` = {vault, toggles, file, persona, memory}，`VIEWS` 8 项 → **正确** |
| 容量门真值 5000 / 3000 / 3000 | `criteria-gate.json.caps` 同值；`distill-write.ts` L58-65 `capMemory ?? 5000`、`capUser/capAgent ?? 3000` |
| 遗忘候选 90 天 | `criteria.json.demote.coldDays = 90`（`criteria.ts` L152 读同键） |
| notes 告警阈值 8,000 / 重排门 200 | `criteria-gate.json.notesWarn = 8000`、`surface.rerank.gate.indexRows = 200` |
| 折叠态生命周期：只在换视图时清 | L3096 `if (currentView !== name) Fold.clear()` → **约束成立** |
| 系统反馈层行号（状态栏/日志面板） | `sc-statusbar` 构建 L3297；`buildLogPanel(150)` L2715（定义 L2642）；`applyLogPanel` L3175 |
| 危险操作二次确认约 8–9 处 | `UI.button` 定义 L980-992 支持 `{danger,confirm,async,busyText,okText}`；`danger:true` 3 处 + 裸 `confirm()` 7 处（L1651/2358/2671/2704/2809/3040/3049）→ **数量级正确** |
| 参数页 620 行、记忆页 ~280 行是主战场 | `renderViewToggles` **1289-1908 = 620 行**（精确命中）；`renderMemoryExpanded` 2186-2467 = 282 行 |

### ❌ 与现树矛盾（5 项 · **须从落地清单中删除**）

| ⑦ 原文断言 | 实测 | 判定 |
|---|---|---|
| 「`/inject/preview` 无 UI 入口 —— 34 个端点中唯一完全无入口者」 | `client.js` **L1438** `api('/inject/preview')`，位于 `renderViewToggles`（参数页 token 估算，可达） | **假** |
| 「`/llm/models` 无入口」 | **L1743** `api('/llm/models')`，位于 LLM 卡下拉（`renderViewToggles` 内） | **假** |
| 「`/config/recent` 无入口」 | **L1974** `api('/config/recent')`，位于 `renderViewYaml`（配置原文页「最近改动」） | **假** |
| 「`/criteria` 只用了 2 个字段」 | 两处调用共用 **≥7 组字段**：L2236-2238（`ledger.rows`/`bankGit.commits`）+ L2919-2923（`version`/`surface.fusion`/`surface.threshold`/`rerankGate{ready,indexRows,threshold}`/`bankGit.lastAt`） | **假** |
| 「运行观测缺进度组件（C1 进度条未迁移）」 | `renderViewObserve` **L2610** 已用 `UI.progress(id)`（`Prog` 由 `apiCtx` L1188-1194 驱动） | **假** |
| 「`/selfcheck` GET 与 POST 混为一谈」 | **L2996** GET 读上次结果、**L3010** POST 触发，已分列 | **假** |
| 「`sc-persona-cell` 样式存在但 JS 未使用」 | **L1323 / L1356 / L1779** 三处在用（persona 四档 L1320、level 五档 L1353、融合策略三档） | 证据**假**（结论「原型里那个『画像密度』控件是编造的」仍成立，但理由须改写） |

> 交叉印证：`node scripts/check-ui-contract.mjs` 实测 **8 PASS / 0 FAIL**，其中第 ④ 项 **34 端点 34 有入口 / 0 无入口**。即**现状无「死角入口」**，v4 声称的 3 个缺失入口在门禁与源码两侧都不存在。

### ⚠ 口径不齐 / 证据不可复验（须修订说明，不影响设计）

| 项 | 问题 |
|---|---|
| ② 表「函数 · 行数」列 | 口径混用：有的是**函数跨度**（`renderViewSettings` 61 ✓、`renderPersona` 36 ✓），有的是**页面总宽**（`renderViewObserve` 114 写成 154 = 含 `buildLogPanel` 40 行；`renderSuite` 22 写成 83 = 含其后 3 个助手函数）。精确值：observe 2601-2714、suite 2816-2837、runExtras 2899-2954、deepSleep 2955-3086 |
| ③「来源：`src/panel.ts` 全部 `route()` 注册」 | **方法已过期**：A–D 根治后端点散在 `panel-shared/-config/-memory/-observe/-inject`，`check-ui-contract.mjs` L25-28 已明写「只扫 `panel.ts` 会得到 0 个端点的假结论」。端点数 34 本身正确 |
| ⑨「`distill.ts:663 capMemory ?? 5000`」 | `distill.ts` 现已 321 行（阶段 C 拆分），该行真身在 **`distill-write.ts` L58-65** |
| ⑬「48 项文案对账（新增 `audit-btn.cjs` 机械扫描）」 | **该脚本在仓库与 `%TEMP%` 全域不存在** ⇒ 这份对账**不可复验**（违反「结论须与证据同源可复核」） |
| 渲染观测量（请求总数 2,457 / P95 320ms / 台账 1,284 行 / 会话数…） | 原型自述为示意值，无权威源；`/criteria` 的 `version` 真值 = `criteria-gate.json` 的 **`v2.2.0`**（原型写「判据版本 v9.1」，是把「蒸馏裁决契约 v9」误当判据版本） |
| 参数取值「活性阈值 0.35 / 熟悉度阈值 0.65」 | **真源核对**：活性模型是**天级**不是单一阈值——`activity.ts` L81-84 缺省 `warmDays 14 / coldDays 44 / archiveDays 90 / hotHits 5`；`criteria.json` 中**无 0.35 这个键**（0.35 只出现在 shadow-sim 的 α 示例里）。MCL 熟悉度阈值真值 = **`mcl.familiarThreshold 0.58`**；0.65 是 recall 绝对余弦 `threshold.tOn`（另一个语义） |
| 「重排门 1,204 / 200 · 已就绪」 | `criteria-gate.json.surface.rerank` = `{enabled:false, gate:{indexRows:200, followRateDropRuns:2}}` → 除行数门**还有连败门**，且当前 `enabled=false`。「已就绪」**过强** |

### 🔁 方案内部自相矛盾（须先收敛）

1. **记忆库 Tab 数**：② 与 ⑤ 写 **4 Tab**，⑧ 写 **5 Tab**（索引/候选/笔记/归档区/统计）；原型实际画出 5 个。
2. **运行观测形态**：① 写「3 Tab」，② 写「保留独立页」。
3. **熟悉度与通道自相矛盾**：总览画「熟悉度 0.72 · 慢通道」，但同一份方案的约束是「高于阈值走快通道」且阈值真值 0.58 ⇒ 0.72 应是**快通道**；原型两处打架。
4. **端点半数表 vs 现实归属**：③ 把 `/criteria` 归「总览 / 参数」，实测该卡在 **`renderRunExtras` L2899-2954，由 `renderDeepSleep` L2958 调用**（现挂在**深度睡眠页**）⇒ 归属迁移必须在改动清单里写死，否则 Tab 化迁移时会丢卡。

---

## 二、设计层评估（值得保留的部分）

1. **首屏换血（配置原文 → 运行总览）方向正确**：现状默认 `'file'`，用户打开面板第一眼是 YAML，这是最该改的一处；新首屏数据全部来自现有端点，**零新增端点**。
2. **两处长页 Tab 化命中要害**：909 行（620 + 282）约占视图渲染代码 2/3，是认知负荷唯一集中点；「只换外层容器、不动 `makeToggle`/`numSetting` 工厂」的切法风险可控。
3. **⑨⑩ 两轮数值/重复清理是真功夫**：容量门 20,000→5,000、6,000→3,000、请求总数撞数、`.dist` 双定义、按钮 8 处同名「执行」——这些是**原型自身**的缺陷，已修，值得保留为落地验收点。
4. **⑫ 的设置项对账**（8 项逐项 vs `Cfg.DEF` L775-784）与 ⑬ 的响应式/危险态/忙碌态盘点**基本准确**，可作为落地自检表。
5. **v4 的「新元素」约一半已在 `client.js`**（令牌/六层 CSS/响应式/日志面板/设置页 8 项/危险确认/忙碌态/进度/深链）⇒ 落地工作量**远小于**原型观感，但也意味着**不能按原型重画**，必须做等价替换。

---

## 三、落地风险（v4 清单之外，必须补的 6 条）

| # | 风险 | 证据 / 处置 |
|---|---|---|
| R1 | **新增设置项不受门禁保护** | `check-ui-contract.mjs` L134 的 `CFG_KEYS` **硬编码 8 项**。v4 新增「启动时视图」（含深链优先级）⇒ 必须同步该数组，否则新键既无门禁、也可能「可配没入口」 |
| R2 | **门禁 ④ 是文本包含判定，抓不住真缺口** | L83 用 `clientSrc.indexOf("'" + ep)`，**注释里出现即算有入口**。真正的「调用点不可达/死代码入口」它抓不到，而这正是 v4 ⑦ 那类误判的温床 ⇒ 建议升级为**调用点判定**（AST 或至少要求 `api('<ep>'` 形式） |
| R3 | **新 CSS 必须与 JS 同批落地** | `test-css-usage-gate.mjs` 会反向证伪：①零使用规则 ②同层重复选择器 ③JS 用了未定义类 ⇒ v4 新增的 `sc-tab` / KPI / 健康条 / 锚点 / `hist` 必须**一次配套**，否则 `npm test` 直接红 |
| R4 | **`client.js` 不在棘轮覆盖内** | `audit-wiring`(I1/I2) 与 `audit-fnspan` 实测基线 0/0、最大函数 350 行，但**扫描面是 `src/*.ts`** ⇒ 拆 Tab 时 `client.js` 可以继续膨胀而门禁无感。建议顺手把 render 函数跨度纳入棘轮（哪怕只做报告态） |
| R5 | **UI 生效链路的真实成本未复验** | 插件声明 `dsh.client.platform = web`、`exports './client' → ./lib/client.js`，即**浏览器侧加载的是安装副本**。v4 ⑤ 称「热重载只换 host 侧，前端须重启 DSH（13:20 实测）」。**本次未复验（重启会中断当前会话）** ⇒ 落地前用「替换安装副本 → 仅刷新页面 → 看面板是否变」实测一次；若确需重启，建议先搭**本地渲染 harness**（headless 浏览器 + mock RPC 直跑 `client.js`），把「改一次重启一次」降为「改一次截图一次」 |
| R6 | **危险态/忙碌态应统一而非新增** | `UI.button` 已支持 `{danger,confirm,async,busyText,okText}`，但只有 2 处用它（L2629 / L2708），另有 7 处裸 `window.confirm` ⇒ 落地应**收敛到 `UI.button`**，不要照原型再写一套确认实现 |

---

## 四、唯一待你裁决项（其余均可自行推进）→ **已裁决（2026-09-13）**

> **裁决结果（2026-09-13，用户）**：采纳 **(a) 恢复百分比 + 容量条**。落地点 `client.js` L2159-2166，
> **须同步改写 L2159-2161 注释**（现文写「不含百分比、不含进度条、不含健康度标记」，不改即注释与实现互指为假）。
> 记忆板块的容量健康卡维持移除（另一条 2026-09-11 拍板，L2304-2305）⇒ 容量展示收敛为「总览 1 条摘要 + 画像页专属卡」两处。
> 定稿原型见 `deliverables/ui-redesign-v6-2026-09-13.html`（含 A–G 台账）。
> **口径更正（2026-09-13 用户）**：去重只查**同一页面内**的重复入口/重复展示；本文 §五 中「按归属页唯一化」的
> 跨页收敛**不采纳**（各页上下文不同即保留）。v6 已按同页口径重做，并一并修正记忆库容量条
> （只有真容量门 `capMemory` 的 MEMORY.md 给百分比条，`notes/` `pending` 无门 ⇒ 去掉伪百分比）。

**画像容量卡：百分比 / 容量条 要不要恢复？**

- 现状代码注释 `client.js` L2159-2161 写：「v9.1 画像容量卡（2026-09-11 用户拍板：**框要留**）：只给静态数字『已用 / 上限 + 画像条数』，**不含百分比、不含进度条、不含健康度标记**（那三类已按用户要求移除）」。
- v4 ⑤ 主张：该注释把「你没否认过的画像条目」与「冷热分布 / 超 R 节 / 指针健康」一并剔除，属**过度修正**，故已按 A 方案恢复百分比与容量条。
- 两者**必有其一为假**，且都要改代码或改注释——**需你一句话定性**：
  - (a) 恢复百分比 + 容量条（采纳 v4 A 口径）⇒ 落地时同步改写 L2159-2161 注释，避免注释与实现互指为假；
  - (b) 维持静态数字（以注释为准）⇒ v4 撤回该项，其余照落地。

---

## 五、落地清单修订（在 v4 的 P1/P2/P3 上增删）

| 批次 | v4 原动作 | 修订 | 理由 |
|---|---|---|---|
| P1 | 补 8 个独立图标 | **保留** | 5 个图标供 8 视图复用属实 |
| P1 | 新增「运行总览」首屏 | **保留**，且**同时**把默认视图从 `'file'` 改为 `overview`（`Cfg` 新增「启动时视图」项） | 现状 L3090 默认是 YAML，不换默认则首屏换血等于没做 |
| P1 | 配置原文下沉 设置→高级 | **保留**，并确认门禁 ④ 仍命中（`/config` `/save` 字符串保留在设置页） | 端点入口不能移丢 |
| P1 | 补 `/inject/preview`、`/llm/models`、`/config/recent` 三个入口 | **删除**（已存在：L1438 / L1743 / L1974） | 避免重复建设 |
| P1 | 补「执行进度」卡（称现状未迁移） | **删除**，改为**把 L2610 已有进度组提升/复用**到总览 | `UI.progress` 已在 observe 视图 |
| P2 | 参数 620 行 → 4 Tab；记忆 282 行 → Tab 化 | **保留**，但 Tab 数须先定稿（4 还是 5），并与 ⑧ 对齐 | 方案自相矛盾 |
| P2 | 记忆库 Tab 化时保 Fold 生命周期 | **保留**（L3096 是唯一清点，Tab 切换**不得**触发 `Fold.clear()`） | 已核实的硬约束 |
| P3 | 画像成熟度分布 / 装配矩阵表 / 最近会话 | **保留**（纯新增展示） | 低风险 |
| — | `/criteria` 卡「补字段」 | **删除**（已用 ≥7 字段），只做**归属迁移**：总览 ← `renderRunExtras`（现挂深度睡眠页 L2958） | 现状与新方案归属不同 |
| — | 新增：门禁同步（`CFG_KEYS` / CSS gate / ④ 判定升级） | **新增** | 见 R1–R3 |
| — | 新增：数值取真源（判据版本 `v2.2.0`、MCL 阈值 `0.58`、活性 14/44/90/5、重排门双条件） | **新增** | 见 ⑨ 修正表 |
| — | 新增：危险确认统一到 `UI.button` | **新增** | 见 R6 |

---

## 六、本次复核跑过的证据（可逐条复算）

```
node scripts/check-runner.mjs        → PASS（30 pass · 1 xfail · 0 skip）
node scripts/check-ui-contract.mjs   → 8 PASS / 0 FAIL（端点 34/34 有入口）
client.js                            → 3502 行（node split 口径；PowerShell Get-Content 因编码会数错，勿用）
git status                           → client.js 与 HEAD 9f6c920 一致（审查期间未改动）
```

**关键行号速查（node 口径）**：`Cfg.DEF` L775-784（navWidth L777）· `applyNavWidth` L3168-3174（兜底 216 在 L3172）· `currentView` L3090 · `Fold.clear` L3096 · `UI.button` L980-992 · `UI.progress` L2610 · `Prog` L1188-1194 · `buildLogPanel` L2715 · `applyLogPanel` L3175 · 状态栏 L3297 · 深链 `location.hash` L3313 · `renderViewToggles` 1289-1908 · `renderMemoryExpanded` 2186-2467 · `renderViewObserve` 2601-2714 · `renderViewSettings` 2755-2815 · `renderSuite` 2816-2837 · `renderRunExtras` 2899-2954 · `renderDeepSleep` 2955-3086。

> 审查人：工程保障（agent）· 关键裁决项见 §四，请由人类负责人拍板后再进入落地。
