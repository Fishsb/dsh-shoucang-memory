# 守藏 · 治理文档审查（2026-09-12）

**审查对象**：项目治理与文档体系（`AGENTS.md` / `CHANGELOG.md` / `CLAUDE.md` / `HANDOVER.md` / `README*.md` / `docs/` / `skill/docs/history/`）
**背景**：本轮可靠性专项发现大量"文档与实现不符"，故延伸审查治理文档
**结论**：**核心问题不是文档写错，而是"实现领先、文档滞后"**——已修 3 项，2 项待决

---

## 📌 TL;DR

- **整体**：文档体系骨架健康（历史隔离做得好），但**新增实现未同步进文档**
- **已修**：`AGENTS.md` 脚本清单漏 **11 项**（全是本轮关键门禁）；`CHANGELOG` 缺 **G-4a** 与 **G-23** 两条 🔴 记录
- **待决**：`CHANGELOG` 中 **G-16 重复条目 ×2**（建议合并，未擅自删）
- **新增**：`AGENTS.md` 补**规则 6（测试登记纪律）**——本轮 N1 教训的制度化

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|---|---|
| 审查文件数 | 8 份根级文档 + 2 个历史目录（16 个历史文件） |
| 发现问题 | 5 项（已修 3 · 待决 1 · 建议 1） |
| 最重问题 | `AGENTS.md` 未记录 11 个脚本，含 4 个 🔴 修复的回归测试 |
| 做得好的 | 历史 ADR 隔离（5/6 标注）、`HANDOVER.md` 过时声明 |

---

## 1. 已修问题

### 1.1 🟠 `AGENTS.md` 脚本清单漏 11 项（**已修**）

`AGENTS.md` 的 `scripts/` 表格列出 21 个脚本，实际有 **32 个**。漏掉的 11 个**恰恰是本轮最关键的那批**：

| 漏项 | 对应 |
|---|---|
| `test-deepsleep-verdict.mjs` | **G-16 + G-19，42 条断言** |
| `test-watermark-guard.mjs` | **G-20，39 条断言** |
| `test-treeops-rm.mjs` | G-18 rename/merge 覆盖 |
| `test-wiring-gate.mjs` + `test-wiring-gate-ast.mjs` | G-A1 接线层（文本版 + AST 版） |
| `check-srcmap.mjs` | src↔lib 导出符号漂移 |
| `check-memory-write-path.mjs` | 记忆写入路径旁路（AST 符号级） |
| `check-installed-sync.mjs` | 已安装副本漂移（报告态） |
| `test-atomic-write.mjs` | D3 原子性 |
| `atomic-fault-injector.mjs` | `test-atomic-write` 的故障注入依赖 |
| `test-carrier-layers.mjs` | — |

⇒ **下一个人读治理文档会完全不知道这些门禁存在**，可能重写或绕过它们。

**修复**：表格补全为 32/32，并标注 `check-runner.mjs` 为统一入口、`atomic-fault-injector.mjs` 不可单独删。

> **⚠ 重要边界**：`AGENTS.md` 在 `.gitignore` 第 2 行，**不进版本库**（刻意设计，见 §4）。
> 故本次修复为**本机生效**，不会推送——这是正确的，不应 `git add -f` 强制入库（会违反规则 1 的本机路径红线）。

### 1.2 🟠 `CHANGELOG` 缺失 G-4a / G-23 条目（**已修**，违反规则 4）

规则 4 要求"用户可感知改动 → CHANGELOG.md [Unreleased] 记一行"。但：

- **G-4a**（🔴，影响 45 段 / 19 会话 / 1,213,582 seq）——**无条目**
- **G-23**（门禁层，🔴 已关闭）——**无条目**

⇒ 唯一未修时被定级最高的产品缺陷，其根治过程在 CHANGELOG 中**完全无记录**。已补两条完整条目（含量化影响面、真根因、`forced=0` 澄清、死循环方案）。

### 1.3 🟡 新增规则 6：测试登记纪律（**已修**，治理制度化）

把本轮 N1 教训写成规则：

> **任何行为级测试/机检件必须登记进 `check-runner.mjs` 的 `CHECKS`，未登记 = 等于没写。**
> 实测三件（`test-deepsleep-verdict` / `test-watermark-guard` / `test-atomic-write`）曾既不在 `CHECKS` 也不在 `npm test` 链上，写了从未运行。
> 登记了但文件缺失 ⇒ 判 FAIL 而非静默跳过。
> 同理：导出函数接了线 ≠ 有效，重命名后必须同步 grep 消费方（含测试件 import）。

规则条数由 5 改为 6。

---

## 2. 待决问题

### 2.1 🟡 `CHANGELOG` 中 G-16 出现 **2 条重复条目**

两条均以「深睡原则落盘失败被判「已消化」（G-16，2026-09-12）」开头，描述同一次修复，但细节有出入：

| | 条目 A | 条目 B |
|---|---|---|
| 措辞 | 崩溃型**静默**丢料 | 崩溃型丢料 |
| 断言拆分 | producer 侧 **7** 条 + 判据侧 **3** 条 | producer 侧 **6** 条 + 判据侧 **4** 条 |
| 验收数字 | 35 PASS / 1 FAIL → 36 PASS | `npm test` exit 0（41/30/18/18/36 PASS） |

⇒ 同一次修复被记了两遍（可能对应 `ce8cdae` 与 `4af10cc` 两个提交）。**未擅自删除**——删文档历史比加条目风险高，且二者细节略有差异，可能各有出处。

**建议**：合并为一条，保留更完整的条目 A，将条目 B 的独有信息（验收数字）并入。

---

## 3. 建议项（低风险）

### 3.1 🟢 `docs/history/README.md` 无历史标注

`docs/history/` 下 6 个文件中，5 个含「历史 / 已废弃 / 不再维护」标注，**仅 `README.md` 没有**。

⇒ 该文件是目录说明，风险相对低，但存在被误当现行规则的可能。建议在文件头加一行历史声明。

---

## 4. 做得好的（保留，勿改）

| 项 | 说明 |
|---|---|
| **历史 ADR 隔离** | `docs/history/` 的 ADR-0001/0002/0003、`F-shoucang-facts.md`、`_template.md` **5/6 含历史标注**，明确"不再维护、仅供查阅"。治理插件移除后未留下误导性现行规则。 |
| **`HANDOVER.md` 过时声明** | 文件头明确标注"⚠️ 历史文档（2026-09-06）……现行工作约定以 `AGENTS.md` 为唯一权威"，并说明三位合并与 pmg 移除背景。**交接文档自标过时，这是正确的做法。** |
| **`AGENTS.md` 治理插件移除说明** | 明确记录"project-map-governance 已按用户拍板整体移除出架构"，避免后人去找已删体系。 |
| **⭐ Agent 文档与公开文档分离** | `.gitignore` 第 2–4 行刻意忽略 **`AGENTS.md` / `CLAUDE.md` / `HANDOVER.md`**（含本机路径 `D:\FF\shoucang`），而 `README.md` / `CHANGELOG.md` / `LICENSE` 正常入库发布。**这直接落实了规则 1「零硬编码本机路径红线」——把含本机路径的 agent 工作文档挡在公开仓外，是正确的治理设计，不应"修正"。** |
| **`.gitignore` 注释** | 写明"lib/ 构建产物按 dsh 插件仓惯例提交入库，不在此列"，解释了反直觉的约定。 |

---

## 5. 审查方法（可复用）

1. **清单 vs 实际比对**：把文档列举的条目与文件系统实际内容做差集（本次抓出 11 项漏项）
2. **规则倒查**：用项目自己声明的规则（如规则 4）反向检查文档完整性（本次抓出 G-4a / G-23 缺失）
3. **重复检测**：统计关键编号在文档中的出现次数，>1 即疑似重复条目（本次抓出 G-16 ×2）
4. **过时标注检查**：历史/交接文档必须有明确的"非现行"声明
5. **修后复验**：改完文档重跑全链门禁（本次 `npm test` exit 0）

---

## 6. 治理文档对齐（续，提交 `3f4c575`）

按"对齐"要求，把上节待决与建议项全部落实，并把三方（本地 / 远端 / 已安装副本）拉齐。

### 6.1 已落实

**① CHANGELOG 去重 G-16（待决项 → 已办）**
两条重复条目（行 14 / 15，同在 `[Unreleased]`，总断言数同为 10、反向证伪同为 35/1 → 36，仅在 7+3 vs 6+4 拆分与措辞上有出入）**合并为一条**。合并时保留两者独有信息（producer 断言列举 + 验收数字），并**回避有争议的 7/6 拆分**，只写可核对的总数 10。
⇒ G-16 条目数 **2 → 1**。

**② 历史目录 README 重写（建议项 → 已办，且比原判断更严重）**
原判断是"缺历史标注"，实际复查发现**更严重**：文件仍在教人用 `node <skill>/scripts/adr.mjs …` 新增 ADR，而 **ADR 机制已随 `project-map-governance` 移除而停用、该脚本已不存在** ⇒ 这是**会主动误导**的文档，不只是缺标注。
两个 `history/README.md`（`docs/` 与 `skill/docs/`）均改为明确的"历史归档 / 不得新增 / 不得作为现行依据"声明，并补时间线提醒（决策早于 09-08 三位合并与 09-11 重新上线）。

**③ 历史标注 12/16 → 16/16**
为 `skill/docs/history/` 的 ADR-0003 / 0005 / 0006 / 0007 补历史归档横幅。

### 6.2 三方对齐结果

| 面 | 状态 | 证据 |
|---|---|---|
| **本地 HEAD** | `3f4c575` | `git rev-parse HEAD` |
| **远端** | `3f4c575` ✅ | 只读 `ls-remote` 读回一致 |
| **profile 钉版** | `3f4c575` ✅ | `~/.dsh/profiles/web/package.json` |
| **node_modules `skill/docs/history/README.md`** | ✅ | md5 `fc9b415…` 与仓库一致 |
| **记忆库 `docs/history/README.md`** | ✅ | md5 `fc9b415…` 与仓库一致 |
| **全量 `lib/`（15 个 .js）** | ✅ **15 / 15** | md5 逐文件比对，**复采 2 次均一致** |

### 6.3 过程中被门禁拦下的一次（记录）

改完 `skill/docs/history/` 后 `npm test` **转红**——`check-deploy-sync` 报 `❌ 不一致 1：skill/docs/history/README.md`，exit 1。

⇒ **门禁这次是对的**：它抓到了"改了仓内、没同步到记忆库"，正是 G-16 那类病的同型。**没有跳过也没有放宽门禁**，而是按门禁要求把文件同步到 `~/.dsh/skills/managing-memory/docs/history/`，之后转 PASS。

另：`lib/client.js` 再次出现 pnpm 不刷新（旧 167,663 B vs 新 169,994 B），按既定方法修复——**先 `rm` 断硬链接再 `cp`**（直接覆盖会污染 pnpm 共享 store）。

---

## ⚠️ 已知局限

- 未逐字审读 `README.md` / `README.en.md` 全文（6 KB × 2），仅核对存在性与时效
- 未审查 `docs/` 下 73 个文档的内容准确性（超出本次范围）
- G-16 重复条目未合并，待确认后再动

---

> 本报告由工程保障团队生成，关键决策请由人类工程负责人复核。
