# 守藏 蒸馏/深睡 可靠性专项 · 第三轮（2026-09-12）

**日期**：2026-09-12
**工作流**：工作流 1（综合代码审查）变体——可靠性专项
**参与成员**：Zhen（工程督导·主理人）、Archi（系统架构师）、Cody（代码审查师）、Rex（SRE）
**承接**：`reliability-distill-deepsleep-2026-09-11.md`

---

## 📌 TL;DR（执行摘要）

- **用户对我上一轮两处结论的质疑全部成立**，已修订口径（见 §1）。
- **新发现 G-17（孤儿 backlog 永久滞留）🟠——本轮最重**：水位表 54 个 sid 中 **12 个（22.2%）** 的会话文件已不存在；这 12 个 `lastSeq` 合计 **872,116**，**6~7 天零推进**；且 **12/12 缺 `fp` 字段** ⇒ 走 `unverifiable-legacy` ⇒ **即便会话复活也不回补**。
- **G-16（深睡原则落盘失败被判「已消化」）已修复并提交 `ce8cdae`**，三层修复 + 10 条新断言，**反向证伪通过**（缺陷态 35 PASS/1 FAIL/exit 1）。
- **G-15 维持 🟠，不升 🔴**：并发实测确证 1 例，但该轮双方均未碰 `AGENT.md`/`USER.md`，不满足我预设的升级必要条件；9445 端口冲突根因**已于 09-10 修复、39h 零复现**，**不列为现行故障**。
- 严重度分布：🔴 0 / 🟠 2 / 🟡 若干。阻塞项 1（部署未完成，**因 github.com 不可达**）。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| 整体评级 | 🟡 有条件通过 |
| 阻塞项数量 | 1（三个提交未推送，纯网络原因） |
| 新增最重缺陷 | G-17 孤儿 backlog 永久滞留（🟠） |
| 已修待部署 | G-16（commit `ce8cdae`） |
| 关键行动项 | 5 条 |
| 建议下一步 | 先定 G-17 方案，再**一次性**部署三个提交（避免多次 re-pin + 热重载） |

---

## 1. 我上一轮的两处错误（用户质疑成立，已修订）

用户提出两点质疑，我初步核实后**全部成立**：

| # | 我上一轮的结论 | 用户的质疑 | 核实结果 |
|---|---|---|---|
| 1 | 「端口未启用导致失效」是**伪命题**（蒸馏不走 HTTP） | 蒸馏真正依赖的是**子代理**，实例没起来子代理跑不了，怎么不是问题 | ❌ **我错了**。只查了"有无 HTTP 调用"这一层。真实依赖是 `ctx.subagents.start('spawn', ...)`（蒸馏 `:1263`、深睡 `:2525`）。代码自承悖论：`:2174-2175`「深睡在全部会话停滞时触发，此时 `roots()` 常为空，而宿主 spawn 必须有 parent → **必然会睡的时候必然崩**」，`:2520` 直接 `log('deep sleep: 无可用 parent agent，跳过本轮')` |
| 2 | 打断**不会丢**（下一轮会带上） | 会话失败后**再也不启用**，"下一次"永不发生 | ⚠️ **有条件成立**。`:3045` 注释自承「另立持久会话清单（**本档未实现**）」；`sweepBacklog` 只遍历 `ctx.agents.roots()`。用户的反例正好落在候选集之外 —— 且实测已量化为 G-17 |

**四问修订后口径**：

| 问题 | 上一轮 | 修订后 |
|---|---|---|
| 实例没启用会不会失效 | 伪命题 | ❌ 错。真实依赖子代理运行时，是**可达失效路径** |
| 执行中被打断会不会丢 | 不会丢 | ⚠️ 仅当会话仍在 `roots()` 内；重试**有上限**（`MAX_DISPATCH_RETRY`，超限强制推进水位并审计记账） |
| 会不会积累 | 不会 | ❌ **会**。孤儿 backlog **无任何回收机制**（G-17） |
| 会不会重新拉起 | 会 | ⚠️ 只覆盖**活会话**；会话消失即永久滞留 |

---

## 2. G-17 🟠 孤儿 backlog 永久滞留（本轮最重，实测数字）

**证据戳**：`src/distill.ts` 3180 行 / 226080 字节，Cody-loss 读于 01:15；水位表与会话文件实测同批。

### 机制

1. `sweepBacklog` 的候选集**只有一个来源**：`:3045` 的 `ctx.agents.roots()`（活会话）。
2. `:3045` 注释自承：「旧注释『重启前已结束的一律补』与现码不符；真要补需会话重新载入，或**另立持久会话清单（本档未实现）**」。
3. **全仓无任何启动扫描或持久会话清单**，能枚举已结束/未载入的 sid 并补蒸馏（Cody-loss 全仓检索确认）。

### 实测量级

| 指标 | 数字 |
|---|---|
| 水位表 sid 总数 | **54** |
| 会话文件已不存在的 sid（孤儿） | **12（22.2%）** |
| 这 12 个 sid 的 `lastSeq` 合计 | **872,116** |
| 推进状态 | **6~7 天零推进** |
| 缺 `fp` 字段的孤儿 sid | **12 / 12（100%）** |

### 最严重的一点：即便会话复活也不回补

12/12 孤儿 sid **缺 `fp` 字段** ⇒ `resolveWatermark`（`:673`）判为 `unverifiable-legacy` ⇒ `discardWatermark`（`:652`）**直接跳到当前边界**。

也就是说：**不是"等会话复活就能补"，而是复活了也补不回来**——补蒸馏的起点被重置为"现在"，历史缺口被永久跳过。

> 注意：`872,116` 是 `lastSeq` 的**算术求和**，跨会话不可比，**不能读作"丢了 87 万条"**。它的意义是量级指示（这批 sid 停在高水位且有 6~7 天未推进），不是精确损失计数。实际滞留事件数需按会话逐个比对末条 seq 才能得出，而会话文件已删 ⇒ **不可考**。这一点必须如实标注，不能拿求和当计数误导。

---

## 3. G-16 🟠 深睡原则落盘失败被判「已消化」——已修复（`ce8cdae`）

### 缺陷

`applyPrinciples` 内 `try { renameSync(tmp, target) } catch {}` **空吞异常**后，仍按内存数组算出 `added>0` 返回；而 `deepSleepLanded` 判据只看 `added/attempted`（**不读 gate**）⇒ 判 `landed:true` ⇒ 审计记已消化、水位推进、下轮不再重蒸 ⇒ **静默永久丢失**。

**与 D2 的差别（这是我自己 D2 的覆盖缺口）**：D2 只堵了**拒收型**丢失（`attempted>0 && added=0`）；G-16 是**崩溃型**——拒收型至少在 `rejectedLines` 留痕，**崩溃型零痕迹**。

### 修复（三层）

| 层 | 内容 |
|---|---|
| ① | 抽出并**导出** `commitPrinciples(tmp, target): {ok, err}`，失败**提前 return**——正确性押在 return 字面量上，漏改 `added` 或 `gate` 其中之一在新版里无法表达 |
| ② | 失败路径 `added/replaced` 一并归 0、`gate` 收敛为导出常量 `COMMIT_FAILED_GATE`、留 `log` |
| ③ | `deepSleepLanded` 增失败 gate 否认表（纵深防御——即使上游把 `added` 谎报成 >0 也判 failed） |

**gate 字面量收敛为常量**：原先产出侧与消费侧各写死一个字符串，改任一侧都会**静默脱钩**（判据还在、门禁已失效，不报错不测试红）。

### 测试盲区同步堵上

`applyPrinciples` 是闭包内 `const` 无 export、单测到不了 ⇒ 原断言**只锁判据不锁生产者**（改回去没有任何断言变红）。新增 10 条断言（producer 侧 7 条含 tmp 清理；判据侧谎报拦截 3 条含对照组）。

### 反向证伪（已做）

把 `commitPrinciples` 失败分支改成 `ok:true` ⇒ **35 PASS / 1 FAIL / exit 1**；还原 ⇒ **36 PASS / exit 0**。

**验收**：`npm test` exit 0（41 / 18 / 36 PASS）、`check-hardcode` ✅、`check-deploy-sync` 0 不一致。附带把 `check-deploy-sync` 接进 `npm test` 链末（能自动化就别靠纪律）。

---

## 4. G-15 🟠 维持，不升 🔴（附一层更严的读法）

Rex 实测：并发确证 **1 例**（09-09T22:47:52–22:51:35，重叠 223s）。

按我预设的升级必要条件（**重叠** ∧ **写入目标为 `AGENT.md`/`USER.md`**）：该轮 distill 目标是 `pending-defer`、深睡画像 +0 ⇒ **第二个条件不满足 ⇒ 维持 🟠**。

**但我要加一层更严的读法**：深睡画像 +0 是因为 `write_gate` **拒收**（exit=4 / 9445 端口冲突），**不是因为它本就不写 AGENT/USER**。

⇒ 结论应表述为：并发对 `AGENT.md`/`USER.md` 的覆盖风险属**未观测**，**不是已排除**。把"没观测到"写成"不存在"是本轮要防的第四类假绿。

**9445 根因**：`NODE_OPTIONS --inspect=9445` 被子进程继承 ⇒ 调试端口冲突 ⇒ `write_gate` 把 stderr 当失败。**已于 09-10 修复，39h 零复现** ⇒ 单列为"已修复项"，**不写成正在发生的故障**。

**残余**：`distilling` 与 `deepSleepRunning` 两守卫互不感知 ⇒ 交叉守卫建议并入 G-13/G-14/G-16 同批（成本极低）。

---

## ✅ 行动清单（按优先级）

| # | 行动 | 负责角色 | 紧急度 | 预期完成 |
|---|------|---------|--------|---------|
| 1 | **G-17 方案设计**：持久会话清单 / 孤儿 backlog 回收机制（含"复活也不回补"这一层的修法） | Archi 设计 → 用户拍板 | **P0** | 下轮 |
| 2 | **一次性部署三个提交**（`fff7df1` D2/D3/N2 · `c15ce10` 文档 · `ce8cdae` G-16）——等 github 可达后：push → re-pin → `pnpm install` → 校验 → 用户热重载 | Zhen + 用户 | **P0** | 网络恢复后 |
| 3 | **G-13 只读清洗清单**（AGENT.md 7 行缺 `- ` 前缀 / USER.md 3 条同义边界 / lessons.md 指针歧义）——**零写入**，交用户确认后再动 | Archi | P1 | 本轮产出中 |
| 4 | G-15 交叉守卫（两守卫互认） | Zhen | P2 | 同批 |
| 5 | G-16 部署后**在真库验证**（确认 `landed` 与 `COMMIT_FAILED_GATE` 实际落盘） | Rex | P1 | 部署后 |

---

## ⚠️ 待完善 / 已知局限

- **`archi-dep`（子代理不可用时的失败分支与水位行为）仍在进行中**，本轮未纳入 —— 上表"实例没启用"的机制描述是我初步核实，完整结论待其回报。
- **G-17 的 `872,116` 不可读作丢失条数**（跨会话 `lastSeq` 求和，非同一序列空间）；真实滞留量**已不可考**（会话文件已删）。
- **三个提交均未推送**：github.com 当前完全不可达（`curl` 走代理与不走代理均返回 000，`ls-remote` 21s 超时）。`fff7df1`/`c15ce10`/`ce8cdae` **都是"已提交、未部署"**，运行实例仍钉在 `4ec6dc6`。
- **本轮未触碰任何私人记忆数据**（「数据先问」原则），G-13 只出只读清单。
- Ardot 报告中的行号会随本轮提交漂移：`src/distill.ts` 现 3185 行（G-16 前为 3155）。

---

## 📚 数据来源 & 成员产出索引

- **Cody（代码审查师）**：G-16 报出与复核（`:1677` 空 catch、`:1678` added 来自内存、对照组 `:1724`）；`_find/_suffix/_err` 死代码**核伪**；🔴-2 主写入非原子**自我撤回**（md5 `b2ec9cd2…` 两份一致）；授权纠正请求（成立，已改判）。
- **Archi（架构师）**：G-15 报出（两守卫零交叉引用）；G-16 修法加严（提前 return > 三元；gate 常量化；`applyPrinciples` 闭包不可测必须抽 `commitPrinciples`）；**自承第 5 次误判**（grep `-B2` 窗口漏掉窗口外 2 行的 catch）；G-1/G-13/G-14 自行降级。
- **Rex（SRE）**：G-15 实测（并发 1 例、9445 根因、39h 零复现）；首个版本用"sid 首末条"致 15 例误报**自行作废**；`landed`/`deepSleepReplayable` 零命中——**属实但归因错**（未部署，非修复无效）。
- **Cody-loss（量化）**：G-17 全量实测数字（54 / 12 / 872,116 / 12-of-12 缺 fp）。
- **Zhen（主理人）**：本轮自认 1 处结论错误（"端口伪命题"）+ 1 处覆盖缺口（D2 未堵崩溃型）。

---

> 本报告由工程保障团队 AI 协作生成，关键决策请由人类工程负责人复核。
