# 守藏 蒸馏 / 深度睡眠 可靠性审查报告

**日期**：2026-09-11
**工作流**：可靠性审查（中断防护 / 失败恢复 / 积压与补偿）
**参与成员**：Zhen（工程督导·编排）· Archi（系统架构师）· Rex（SRE）· Cody（代码审查师）
**基线**：`D:\FF\shoucang` @ `4ec6dc6`；运行时数据截至 2026-09-11T15:49Z（本地 23:49）
**方式**：全程只读，未改任何文件、未重启宿主、未 kill 进程

---

## 📌 TL;DR（执行摘要）

- **直接回答你的四问**：
  1. **端口没启用** → 伪命题。蒸馏/深睡**不经过 3080**（进程内 `ctx.subagents.start` + `spawn`），端口没起 = 插件没加载 = 定时器根本不存在，**不是"调用失败"而是"调用者不存在"**。
  2. **执行中被打断** → 有防护且实证有效。22 次 `inactive context` 打断全部「跳过本轮 + 水位保留」，下轮重扫，**不丢数据**。
  3. **失败会积累吗** → **不会**。水位驱动而非队列驱动，失败 = 水位不推 = 同批下轮重试，天然有界。
  4. **会重新拉起吗** → **会**（`sweepBacklog` 启动 30s 首扫 + 每 10min），但**只覆盖当前 ctx 内 live 的会话**。
- **但是**：审查发现 **4 个"看起来有防护、实际无效"** 的缺口；其中被误报为"104 条静默丢失"的那条**经二次复核已翻案**——数据没丢（水位保留会重蒸），真实代价是**重复写入 8 条**。真正会永久丢料的是**深睡重启丢料**（D2）。
- **严重度分布**：🔴 严重 3 · 🟠 高 4 · 🟡 中 4
- **最反直觉的发现**：真正的损失**不在调度侧**（那侧防护扎实），**而在判定侧**——状态机把失败判成了成功。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| 整体评级 | 🟡 **有条件通过**——中断场景主链路防护真实有效；但判定侧存在静默丢料，不得给出"不会丢"的无条件结论 |
| 确认的永久丢料 | **4 条**（深睡重启丢料 D2，实测 08:32/08:48 两轮） |
| 已翻案的误报 | ~~104 条静默丢失~~ → 实为**重试有效 + 重复写入 8 条**（D1，见 §2） |
| 阻塞项 | 0（均为已运行系统的既有缺陷，非上线阻塞） |
| 关键行动项 | 5 条（见文末） |
| 建议下一步 | 按 P0 → P1 顺序修，每条配"杀进程/重载"的**真实形态**回归断言 |

---

## 1. 四问逐条回答

### Q1 定时调用前实例没启用端口 → **伪命题（🟢 无风险）**

- **证据**：`src/**` 全文 grep **无任何 3080 引用**（仅 Ollama `11434` 可选 embedding，且 `try/catch` 降级）。LLM 调用是进程内 `ctx.subagents.start('spawn')`（`distill.ts:1245` / `:2507`），脚本调用是 `spawn(node, script)`，写入是 `node:fs`——**全程无 HTTP**。面板才依赖 `ctx.webServer`，且缺失时 `warn + return`（`panel.ts:89-92`），不影响调度。
- **性质**：不是"跳过一轮"，是**静默黑洞**——停机 3 小时 = 3 小时零巡检、零蒸馏，且**没有任何组件记录"我本该跑但没跑"**（全仓 grep `heartbeat|deadman|missed` 零命中）。
- **真实版本**：日志里有 **端口被占用**（非未启用）的实证案例，见 §4-(a)。

### Q2 执行中被打断 → **🟢 有防护，实证有效**

- **定时器生命周期干净**：深睡巡检（`:3075`）、积压扫尾（`:3105`）、自检（`:3085`）、idle（`:2872`）**全部挂在 `ctx.effect`**，cordis 4.0.0-rc.9 的 `ctx.on` 内部也走 `fiber.effect`（`node_modules/cordis/lib/index.js:337-344`）。热重载**不留幽灵 tick**——这推翻了我派单时的假设。
- **实证 22 次打断全部安全**（`09-09T16:24` ~ `09-10T01:01`）：
  ```
  distill ERROR session-: cannot get required service "subagents" in inactive context  ×9
  distill: 5f024550 段3 旧 ctx 已失效（inactive context），跳过本轮（水位保留 65852，待扫尾）
  ```
  防护动作统一为「**跳过本轮 + 水位保留/回退**」⇒ 下轮重扫，不丢。
- **段级断点续传有效**：`MAX_CHUNKS_PER_RUN=3`（`:43`），**每段成功立即推水位**（`:1308`），段失败 `break`（`:1348`）⇒ 前段不重蒸。19+ 次「下轮续传」实证。
- **跨实例 claim 锁**：`tryClaim`（`:1066-1086`，TTL 25min）防双 fiber 双蒸。

### Q3 失败/无产出是否积累 → **🟢 不积累，但 🟠 无告警面**

- **不是队列型**：蒸馏是**水位驱动**（`audit/distill-watermark.jsonl`），失败 = 水位不推 = 同批下轮重试，**受会话事件流长度天然约束**，不会无限膨胀。
- **实证**：`pending/` 仅剩 1 个 README，`.processed` 77 个（已归档），**当前零积压**。
- **深睡侧刻意不积累**：无痕迹时不调 LLM 也不留审计（`:2321`），38 条审计行 / 2.5 天。
- **但没有任何"未消化"计数或告警**：`panel.ts` grep `未消化|告警|alert|backlog` 仅 1 条无关命中；日志里 116 处「未消化」全是自由文本，无聚合、无阈值、无 UI 提示。
- **一个持续未消化源**：`ws 反解: 转录定位失败` **1023 次 / 16 个 sid**（09-10T12:01 → 09-11T10:23），衍生 130 条 `gate-reject`。降级 pending 不丢弃，但**长期堵在待认领**（累计保留 249 / 仅写入 11，回流率 4.2%）。

### Q4 会重新拉起吗 → **🟠 部分会**

- **✅ 蒸馏有 catch-up**：`sweepBacklog()` 启动 **30s 首扫** + 每 10min（`:3107-3108`）。本次热重载实证（15:39:49Z，重载后 30s）：
  ```
  sweep: 7f37b807 水位 3636→6083 有未消化增量，补蒸馏   ← 一次补回 2447 seq
  sweep: 3f75a0ad 水位 1675→1676 … / 6ba51f20 … / 09bd40e0 … / b0a40b40 …
  ```
- **✅ 深睡无需 catch-up**：它是**状态驱动**（全部会话停滞 ≥3h）而非日程驱动，`lastActivityAt` 从审计回放 ⇒ 停机 8h 后重启停滞钟不重置，首 tick 即触发。**不存在"错过的 N 个周期"**。
- **❌ 覆盖边界**：候选只取 `ctx.agents.roots()`（当前 ctx 内 live 会话，代码注释 `:3001-3002` 自认）。**未重载的会话永久不补偿**（无持久会话清单）。
- **🔴 但判定侧把失败当成功**（见 §2-D1），这是"重新拉起"最大的破口。

---

## 2. 发现的缺口（按严重度）

### 🔴 D1 — `dispatch-failed` 与重复写入（**经二次复核后定性已修正，见下方 ⚠️**）

> ⚠️ **本条定性经过一次翻案，务必读完**：初版（Rex）判定为「104 条静默丢失、不可恢复」，**该定性错误**。Archi 提出异议后，我（Zhen）用「代码分支 + 日志计数」双向复核，**裁定 Archi 正确**。原始统计保留如下，但**结论已改写**。

- **原始统计**（`distill-audit.jsonl`，220 个有结局的蒸馏轮次，Rex 产出，数据本身无误）：

  | fclass | 轮次 | completed | added | failed |
  |---|---|---|---|---|
  | **dispatch-failed** | **52** | **52/52** | 94 | **104** |
  | gate-reject | 49 | 49 | 30 | 0 |
  | ok | 44 | 44 | 120 | 0 |
  | discard | 61 | 61 | 0 | 0 |
  | json-parse | 4 | 2 | 0 | 0 |

- **❌ 错误定性（Rex）**：「`stop=completed` ⇒ 判为 done ⇒ 104 条静默丢失、水位照推、不可恢复」。
  该推断的依据是**测试件**（`scripts/test-*.mjs`）里的状态机表述 `attempted>0 且 stop=completed ⇒ done`，**那不是运行时逻辑**。
- **✅ 正确定性（Archi 提出，Zhen 用代码 + 日志双向复核裁定）**：运行时是 `distill.ts:1308` / `:1314` 两条互斥分支：
  ```js
  if (stop === 'completed' && out && disp.failed === 0) { …推水位… }        // :1308
  else if (stop === 'completed' && out && disp.failed > 0) {                // :1314
    const tries = (dispatchFailStreak.get(streakKey) || 0) + 1
    if (tries >= MAX_DISPATCH_RETRY) { …强制推进 + 落 dispatch-failed-forced… }
    else { dispatchFailStreak.set(streakKey, tries); log('…水位保留，下轮从本段续传') }
  }
  ```
  **`disp.failed > 0` 时水位保留、不推进**——该分支注释明写「A1（**2026-09-11 审查修复**）」，即**今天刚加的防护**。
- **日志实证（决定性）**：
  ```
  第 1/3 次重试: 4    第 2/3 次: 0    第 3/3 次: 0
  强制推进水位: 0     dispatch-failed-forced: 0
  ```
  `第 2/3 次 = 0` ⇒ **每次落盘失败后，下一轮重蒸就成功了**，`dispatchFailStreak` 从未累积到 2，**强制推进路径从未触发**。
- **真实后果**：数据**没有丢**（水位保留 ⇒ 下轮重蒸），代价是**重复写入**——实测 `索引行重复 8 条`。原因是水位保留要重蒸整段，而 `memory-append.mjs` 的 appends 通道**无幂等键**。
- **修法**：给 appends 加内容指纹幂等键（复用已有 `intentTokens`），或宿主 `writeDispatch` 落 stub 指纹后重蒸前比对。**不需要改 `stop` 语义**。
- **同类（这些才是真·不可自动恢复）**：「新索引超容量不写」62 次、「落点失败」24 次、「ENOENT 直写失败」18 次 —— 均需人工介入（合并下沉 / 建顶层锚点 / 建目录）。

### 🔴 D2 — 深睡水位重启即丢料（**Archi 与 Cody 独立得出同一结论，Zhen 已复核**）

- **机制**：`lastDeepSleepAt` 纯内存，重启靠审计回放（`:1402-1419`）。回放过滤：
  ```js
  o.kind==='deep-sleep' && !o.error && o.stop!=='error'
    && !['no-parent','no-traces'].includes(String(o.result))
  ```
  注释写「只回放**确实消化过痕迹**的深睡」——**意图正确**，但排除名单只覆盖「无事可做」，**没覆盖「做了但失败」**。
- **实测**：38 条深睡行中仅 20 条带 `result`，值全是 `no-traces`/`no-parent`。今日五轮：
  ```
  08:32  attempted=3  added=0  gate=all-rejected  result=None  ← 失败
  08:48  attempted=1  added=0  gate=all-rejected  result=None  ← 失败
  08:50  attempted=1  added=1  gate=pass          result=None  ← 成功
  ```
  `String(undefined)` = `'undefined'`，**不在排除名单内** ⇒ 失败轮被当有效水位回放。
- **进程内判据与回放判据不一致**：进程内用 `deepSleepLanded`（`distill.ts:354-358`，看 `added`/`attempted`/`gate`），回放**不看这些**（`:1413-1416`）。
- **后果**：同进程靠内存回滚能重试（08:32→08:48→08:50 三轮实证），**但中间只要重启一次**，那批材料永久出窗。
- **修法**（极小）：回放复用 `deepSleepLanded`；或审计行补 `result: landed?'done':'failed'`。**~2 处小改**。

### 🔴 D3 — 主写入通道非原子（Cody 发现，Zhen 复核后裁定 Archi 说法不完整）

- **事实**：宿主侧原子纪律整齐（`distill.ts` 的 `atomicWrite` 家族 `:808/:1613/:1762`、`treeops.ts:146` 全 tmp+rename）。**但蒸馏 90% 的写入走 `memAppend` → 外部脚本 `memory-append.mjs`**，其末行是：
  ```js
  await writeFile(filePath, content, 'utf8');   // :148 —— 直接覆盖，无 tmp、无 rename
  ```
  仓内与库根是同一份（sha `33df7ab4f823` 一致），**不是部署漂移，是设计如此**。
- **兜底**：写前 `copyFile` 到按分钟戳的备份目录（`:37`，`catch {}` 尽力行为），实测已堆积 **98 个备份目录、无清理**。
- **放大因素**：`memAppend` 走 `runNode` 起**独立 OS 进程**，父进程被杀**不会**连带杀死子进程。
- **修法**：脚本末行改 `writeFile(tmp)` + `rename(tmp, filePath)`；或宿主侧读全文→拼接→走自己的 `atomicWrite`。**1 个脚本 1 行**。

### 🟠 D4 — 并发防护有破口（Cody 发现）

- **`tryClaim` 是 TOCTOU**（`:1070-1079`）：`readFileSync` 判 TTL → `writeFileSync` 写 claim 两步非原子，同 tick 内两个 fiber 可各自拿到锁 → **双蒸馏**。修法：`writeFileSync(f, body, { flag: 'wx' })`（单行）。
- **深睡完全没有跨实例锁**：只有内存 `deepSleepRunning`，无 claim 文件。多代并存时两个实例用同一 `since` 各跑一次归纳；且 `applyPrinciples` 的 tmp 名固定为 `AGENT.md.tmp`（`:1613`），两实例互相覆盖 tmp，可能把对方被拒收的内容 `renameSync` 进正式文件。
- **写侧不幂等**：`memory-append.mjs:129` `lines.splice(insertPos, 0, ...)` **无去重**（`grep dedup` 零命中）。索引行有唯一性硬门（`distill.ts:877-921`，精确/词法 0.66/向量 0.80 三重），**notes 正文 append 没有**。

### 🟠 D5 — 失败重试无上限 / 无退避 / 无死信

- 段失败靠 10min 巡检**无上限重试**；唯一上限 `MAX_DISPATCH_RETRY=3`（`:1059`）只覆盖"落盘失败"子类，且 `dispatchFailStreak` 是**内存态、重启清零**（`:1060`）。
- 无指数退避、无抖动、无 dead-letter。真库某段恒定失败 ⇒ **每 10min × 每个 root 一次 spawn，永不停止**。
- 注：`distill.ts:733` 的 `1500ms × 3` 是 `resolveWorkspace` 的**转录定位**重试，**不是 LLM 重试**（我与 Archi 都误读过）。

### 🟠 D6 — 无可观测性：连调度器死活都判断不了（Rex 实测）

- 无心跳、无死人开关。当前状态：**面板应答 200、`running=3`、`reload fail=0` ⇒ 存活**，但日志已静默 11 分钟，**无任何证据证明它还在 tick**。
- 历史上有 187 段 >6.6min、109 段 >10min 静默（占 6.2%）⇒ 11 分钟静默落在正常分布内，**无法据此判定死亡**。
- **结论：这不是"调度器有问题"，而是"没有任何数据能回答这个问题"**——可观测性缺口本身。

### 🟡 D7 ~ D10

- **D7 审计/水位文件无轮转**：`distill-audit.jsonl` 172KB、`distill-watermark.jsonl` 60KB、`ledger.jsonl` 56KB，全 append-only；`readWatermarks()` 每次全量读（`:582`），每 10min × 9 个 root ⇒ O(filesize × roots) 线性劣化。唯一有修剪的是 `episodes.jsonl`（`EPISODE_CAP=256`）。
- **D8 孤儿 claim**：9 个（5 个来自 09-10），进程被杀时 `finally` 未跑，靠 25min TTL 兜底。
- **D9 采集失败被吞成"真空窗口"**：`gatherDeepSleepTraces` 内所有读取 `catch { return [] }`（`:1443/:1451/:1495`）⇒ 采集异常与真空窗口**不可区分**，同样滑窗 ⇒ **采集失败 = 永久丢料**。
- **D10 双证失效时增量永久丢弃**：22 条 `watermark-invalidated`，直接跳到当前 maxSeq 不回补。代码注释（`:626-632`）论证了「宁可少蒸一次，不可错位重蒸」——**这是合理显式取舍，非 bug**，但值得在面板可见。

---

## 3. 三个"反直觉"结论

1. **219 次启动不是宿主重启，是热重载**。Rex 把 `reload-debug.log` 与「distill 启动」做时间配对：窗口内 reload 130 次，**reload ±30s 内有 distill 启动 = 128/130（98%）**。日志跨度 93.3h，若真是 10min tick 应打出 ~560 条，实际 214 ⇒ **「启动」是装配事件而非 tick**。
2. **审计字段与运行时逻辑不是一回事**：`stop=completed` 只是**子代理的返回码**，审计再单独记 `fclass='dispatch-failed'`。两者从不交叉判定——**判"是否消化"看的是 `stop` + `disp.failed` 的组合分支（`:1308`/`:1314`），不是 `stop` 单值**。Rex 正是误把测试件里的状态机当成了运行时逻辑。
3. **同类缺口真正的代价是"重"不是"丢"**：水位保留 ⇒ 下轮重蒸 ⇒ 数据回来了，但 appends 无幂等键 ⇒ 重复。实测 `索引行重复 8 条`。
3. **"看起来有防护、实际在主通道上无效"**——这是本次审查反复出现的模式：宿主原子写入很整齐，但主通道是外部脚本的非原子覆盖；深睡水位有回放恢复，但回放判据漏掉了最该排除的失败轮。**与今日下午修的 B0（守卫失配）是同一类病**。

---

## ✅ 行动清单（按优先级）

| # | 行动 | 负责角色 | 紧急度 | 改动面 | 预期收益 |
|---|------|---------|--------|--------|---------|
| 1 | **D1**：给 `memory-append.mjs` 的 appends 通道加内容指纹幂等键（复用已有 `intentTokens`），或宿主落 stub 指纹后重蒸前比对 | Cody | **P0** | 中（~30 行） | 消除重蒸导致的重复写入（实测 8 条） |
| 2 | **D2**：深睡回放复用 `deepSleepLanded`；或审计行补 `result: landed?'done':'failed'` | Archi | **P0** | 极小（~2 处） | 消除重启丢料 |
| 3 | **D3**：`memory-append.mjs:148` 改 tmp+rename；备份目录改按日/保留 N 个 | Cody | **P0** | 1 行 | 消除中断留半文件 |
| 4 | **D6**：落 `audit/heartbeat.json`（每 tick 一行），启动对比 `now - last > 2×间隔` 记 gap；面板展示「本实例覆盖缺口」 | Rex | P1 | 小（~20 行） | 让"调度器死没死"可判定 |
| 5 | **D4**：`tryClaim` 改 `{ flag:'wx' }`；深睡加 claim 锁；`AGENT.md.tmp` 改为 `.tmp-<pid>-<rand>` | Cody | P1 | ~15 行 | 消除双蒸与 tmp 互覆盖 |

**每条都必须配一个"杀进程/重载"的真实形态回归断言**——打在「写后推水位前」「深睡审计已落但 added=0」这两个点上。**不要重演今天 A6 的假绿**（断言覆盖了夹具形态却漏掉真库形态）。

---

## 4. 审查过程中的翻案记录（方法论）

本次审查**三次被成员推翻我的前提**，**一次成员间互相翻案**。留痕供后续复用：

| # | 原判断 | 推翻者 | 裁定依据 |
|---|--------|--------|---------|
| 1 | 我假设热重载会留下幽灵 tick | Cody | 定时器全部挂 `ctx.effect`，cordis 的 `ctx.on` 内部也走 `fiber.effect` |
| 2 | 我说 `distill.ts:733` 是 LLM 重试 | Archi + Cody | 实为 `resolveWorkspace` 的转录定位重试；LLM 侧本轮不重试 |
| 3 | 我说 `ledger.jsonl` 是蒸馏账本 | Archi + Rex | 真账本是 `distill-audit.jsonl`；`activity.jsonl` 根本不存在 |
| 4 | **Rex**：`dispatch-failed` 判 completed ⇒ 104 条丢失 | **Archi** | 运行时 `:1314` 分支是「水位保留 + 重试」，**不是**推水位；日志 `第2/3次=0`、`强制推进=0` 佐证 |
| 5 | **Archi**：落盘层单文件原子 | **Cody**（我裁定成立） | 宿主侧确实 tmp+rename，但主通道 `memory-append.mjs:148` 是 `writeFile` 直接覆盖 |

**可复用的经验**：
- **区分「审计字段」与「运行时逻辑」**——`stop=completed` 是子代理返回码，`fclass` 是审计归类，**判"是否消化"看两者的组合分支**。拿测试件里的状态机去套运行时，会得出完全错误的定性。
- **统计数字正确 ≠ 定性正确**：Rex 的 52/52、104 条全部属实，但据此推出的"不可恢复"是错的。**数字要经过代码路径验证才能下结论。**
- **用"零计数"做反证**：`第2/3次 = 0` 和 `强制推进 = 0` 这两个零，比任何"有防护"的声明都更能证明重试机制真的在工作。

---

## ⚠️ 待完善 / 已知局限

- **「端口未启用」零日志证据**：调度器不经 HTTP 自调用，该路径在现有日志中不留痕，**无法证真也无法证伪**。Rex 明确拒绝用推断填补。
- **09-08 的 37 次 distill 启动无法归因**（`reload-debug` 该日无记录）；41% 的启动无 reload 配对，是否存在另一触发源未知。
- **D1 的 104 条丢失是"判定错误"而非"写入失败"的二次确认**：已复核 `failed` 字段确为写入失败计数，但未逐条比对库内是否真的缺失（部分可能已被后续轮次补回）。
- **D3 的重复写入未实测复现**：由「写序非原子 + 子进程独立 + 无去重」三点推理得出，未在日志中找到确凿重复入册样本。
- **深睡 `result` 字段在 09-09 之前的旧格式行中是否存在**：基于 38 条全量解析判定，未逐行确认历史行。
- **当前调度器是否仍在 tick**：无心跳，**不可判定**（这正是 D6）。

---

## 📚 数据来源 & 成员产出索引

- **Archi（系统架构师）**：四问逐条架构答案、生命周期与持久化架构图（标出每步是否落盘/断点在哪/重启能否恢复）、8 条架构缺口（G-1~G-8）、2 条 ADR 建议。**独立发现 D2（G-1）**，并纠正了我 3 处事实（端口模型、`scheduler.ts` 无定时器、`ledger.jsonl` 字段归属）。诚实标注了 4 条未验证项。
- **Rex（SRE）**：785 行审计全量解析、219/214 次启动的**时间配对归因**（98% 命中热重载）、失败分类统计（220 轮 / 全损 5 轮 / 半损 47 轮）、四类真实中断案例（端口被占 / inactive context / 无 parent / 容量满）、当前健康快照、6 项可观测性建议。产出**扎实的统计底座**（220 轮分类、219 次启动的时间配对归因、四类中断案例、6 项可观测性建议），并纠正了我 4 处事实（`activity.jsonl` 不存在、`ledger.jsonl` 非蒸馏账本、深睡配置未落 `scheduler.json`、启动次数归因）。其 D1 的**统计数据无误**，但「104 条静默丢失」的**定性被推翻**（见下节）。
- **Cody（代码审查师）**：定时器生命周期逐项核查（推翻"幽灵 tick"假设）、幂等/原子性/checkpoint/catch-up/重试上限 6 项代码级结论、**4 条"看起来有防护实际无效"专项**、10 条缺口清单（P0×3 + P1×3 + P2×4）。**独立发现 D3（主写入通道非原子）与 D4（并发破口）**。
- **Zhen（工程督导）**：派单与编排；**独立复核**了 D1（52/52 判 completed，failed 104 > added 94）、D2（回放过滤代码 + 今日五轮审计行）、D3（`memory-append.mjs:148` 实读 + 仓/库 sha 比对）；**裁定 Archi 与 Cody 在"写入是否原子"上的分歧**（Cody 正确，Archi 只覆盖宿主侧、漏了主通道）。

---

> 本报告由工程保障团队 AI 协作生成，关键决策请由人类工程负责人复核。

---

## 🔧 第二轮：P0 修复落地（2026-09-12 00:45，commit `fff7df1`）

本轮把首轮识别的 P0 中 **D2 / D3** 落地，并修复审查过程中暴露的**红线闸门假阳性**。三项均配「真实形态实证 + 反向证伪」，未达此标准的不计入。

### ① D2 深睡重启丢料（水位回放判据与进程内判据不一致）

| 项 | 内容 |
|---|---|
| 根因 | 重启时 `lastDeepSleepAt` 从 `distill-audit.jsonl` 回放重建；旧判据只排 `error` / `stop=error` / `result ∈ {no-parent, no-traces}`（"无事可做"），**漏排"做了但被拒"**（`attempted>0 && added=0`） |
| 后果 | 那类轮次被当作有效水位回放 ⇒ 该批痕迹**永久关在窗外**，重启一次即丢。实测 09-11 08:32 / 08:48 两轮共 4 条候选行 |
| 修复 | 审计行新增 `landed: deepSleepLanded(stop, out, app)`（`src/distill.ts:2593`，**与进程内同一函数**）；回放判据抽出为导出纯函数 `deepSleepReplayable`（`:368`），调用点收敛到它（`:1433`） |
| 为何不回捞 | 历史 785 行绝大多数无 `landed`，回落旧判据（保守兼容）。回捞会把水位往回推，**在幂等键落地前只会把"丢料"换成"重复写"** |
| 实证 | `scripts/test-deepsleep-verdict.mjs` 增 11 条断言（**26 PASS / 0 FAIL**），含真库形状 `attempted=3/added=0/gate=all-rejected/landed=false` |
| 反向证伪 | 移除 `landed` 分支 ⇒ 核心断言 ④ 两条**当场变红**；恢复 ⇒ 复绿。**另验证退出码语义**：注入失败 ⇒ 25 PASS / 1 FAIL / exit=1 |

### ② D3 主写入通道非原子（中断留半文件）

| 项 | 内容 |
|---|---|
| 根因 | `skill/scripts/memory-append.mjs:148` 原为 `writeFile(filePath, content)` 直接覆盖；宿主重启/热重载/强制刷新落在「截断后、新内容未落完」之间即留半截主文档 |
| 加剧因素 | 备份虽在（写前 `copyFile`），但**无任何自动回滚**，下一次读写基于损坏副本继续 |
| 修复 | 改同目录 tmp 写 + `rename` 原子替换；失败 `unlink(tmp)` + **exit 5**（调用方 `distill.ts:870` 只判 `status===0`，非零一律计 failed，兼容） |
| 顺带治理 | 备份目录由「分钟级永不清理（实测 **98 个** = 98 份主文档全量副本）」改保留最近 `SHOUCANG_BACKUP_KEEP`（缺省 30，0=不裁剪） |
| 实证 | 临时库写入 exit 0、无 tmp 残留；造 5 个历史备份 + `KEEP=2` ⇒ 剩时间戳**最大 2 个**（本次新写的必留）；`KEEP=0` ⇒ 一个不删 |

### ③ N2 红线闸门假阳性（根因级）

`check-hardcode` 的 `SKIP_DIRS` 未排除 `.workbuddy-ai/`（`.gitignore:35` 已忽略、永不入库的运行时暂存区）⇒ 面板探针把 `root` 路径写入该区即被判红线违规。

**此前的处置是删掉探针文件——属掩盖而非修复。** 假阳性会训练人忽视红灯，比漏报更危险。本次收窄的是"扫哪里"，未削弱"判什么"：**反向证伪已做**（注入真实违规 ⇒ exit 1 并点名文件；清理 ⇒ exit 0）。

### 附带订正（不改行为）

`memory-append.mjs` 文件头称「在该小节末插入条目」，但 `secEnd` 扫描判据 `lv <= lastHead.level` 对非标题行（`headingAt` 返回 0）恒真 ⇒ 扁平小节（正文直挂 `##`）实际插在**正文首行前**，呈「最新在上」倒序。真库 `notes/flows.md §深睡蒸馏` 已验证（今日条目在最前、旧的在后）。**仅订正注释**——翻转会改变全库既有顺序，风险与收益不对等。

### 交叉复核中的翻案（本轮 3 次）

| # | 原主张 | 裁定 | 依据 |
|---|--------|------|------|
| 1 | Rex：D2 修复"完全无效"，审计行无 `landed` | **核伪** | `src/distill.ts:2593` 确有 `landed: deepSleepLanded(...)`；`:368`/`:1433` 确有 `deepSleepReplayable`。Rex 读到的是我做反向证伪时的**临时缺陷态** |
| 2 | Archi：G-1「热重载不重置深睡水位会重复吸收」 | **Archi 自我撤回** | `lastDeepSleepAt` 是 `ctx.effect()` 内闭包变量，随 fiber dispose 销毁，重建为 0 |
| 3 | Cody：`_find`/`_suffix`/`_err` 是死代码建议删 | **不存在** | `grep -c` 在 `src/distill.ts` 均为 **0**；Cody 读到的是旧版本 |

### 我的操作失误（留痕）

1. **同一条消息内对同一文件发两个 Edit** ⇒ 两者基于同一快照写入、后写覆盖先写，追加的断言块丢失，而工具报"成功"。**教训：同一文件不并行 Edit；每次编辑后必须回读验证。**
2. 用 `sed -i` 删重复块时**连带删掉了测试件末尾的 `console.log(结果)` + `process.exit()`** ⇒ 测试件变成"只打印 ❌ 但隐式 exit 0"的**假绿**。已补回并新增退出码语义验证。
3. 在评审进行中把 `src/distill.ts` 临时改成缺陷态做反向证伪，**未广播给正在按行号评审的队友**，导致 Archi/Cody 读到中间态、产生两次误报与一次重复核对。

### 待办（未在本轮落地）

| # | 项 | 级别 | 说明 |
|---|----|------|------|
| D1 | `appends` 通道幂等键 | **P0** | 现存去重**只在 `newIndex` 通道、且按标签**；`appends`（notes 正文）**完全无去重**。需先补跨标签/章节限定的键设计 |
| G-10 | 容量门活锁 | **P0** | MEMORY.md 4715/5000，整合只在深睡；重试**有界（3 次）**非无限——`:1318` 的 `tries` 对容量拒收同样计数 |
| G-11 | 扫尾与深睡并发 | 🟠 | `:3043 void distillAgent(a)` 无深睡互斥；但同段 `:3040 claimHeld(sid)` 已挡同 sid 双蒸，属条件性 |
| N3 | `hasActiveSubagents` 同义调用 | 🟢 | `:1151` 与 `:3032` 同义；`:2799` 是深睡巡检另一用途，不可删。属清理非修复 |
| — | 部署 | — | D3 已同步库内副本（即时生效）；**D2 需 re-pin + `pnpm install` + 热重载**，待你确认后执行 |
