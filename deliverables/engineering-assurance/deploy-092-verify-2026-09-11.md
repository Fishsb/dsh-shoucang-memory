# 0.9.2 部署收口验证 —— 前次「部署漂移」已消解

**日期**：2026-09-11
**工作流**：工作流 4「部署前检查」（Go/No-Go 决策）
**参与成员**：Rex（SRE 工程师，安装面校验）· Cody（代码审查师，制品链逐字节比对）

> **背景**：1 小时前（18:28 左右）复核发现「仓 0.9.2 工作区 vs 线上 0.9.1 + `boundary.js` 已删未同步」的部署漂移，判定为并发会话在途所致，并给出 P0 指令「冻结用当前工作区打包安装」。
> **本次复验**：并发会话已于 18:50 前后**提交收口**，漂移**已完全消解**。本报告为收口后的 Go/No-Go 结论。

---

## 📌 TL;DR（执行摘要）

- **漂移已消解：✅ 全绿。** 并发会话提交 `b07e42d refactor(0.9.2)!: 拆掉工作区边界`，工作树干净。
- **官方校验 9/9 PASS：** 产物 SHA256 一致、声明指向 0.9.2、实体版本 0.9.2、12 个 `core/host` 源文件**逐字节一致**。
- **测试全过：** `npm test` **21/21 PASS / 0 FAIL**（提交方声明四套合一 **98/98**）。前次 `ERR_MODULE_NOT_FOUND` 已消失，**反证其确属并发在途态、与我的清理无关**。
- **决策：🟢 Go —— 但**唯一剩余动作是**重启 dsh-web**（禁 agent 自行重启，须由人执行）。
- **新增待办：** 0.9.0 / 0.9.1 的 tgz 与配对脚本（7 项）现已**全部 superseded**，且均处于治理模型的 `unregistered` 桶 → 可纳入下一轮清理。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| 整体评级 | 🟢 **通过**（安装面全绿，Go） |
| 阻塞项数量 | 0 |
| 剩余人工动作 | 1（重启 dsh-web，agent 禁止自行执行） |
| 关键行动项 | 3 条 |
| 建议下一步 | 由人在终端重启 dsh-web → 新会话验证工具数 = 6（旧版 11） |

---

## 🔍 检查清单逐项打勾

官方脚本 `verify-0.9.2-install.ps1`（只读）实测输出：

| # | 检查项 | 结果 | 实测值 |
|---|--------|------|--------|
| 1 | 产物存在 | ✅ PASS | `D:\FF\project-nav\dsh-external-project-nav-0.9.2.tgz` |
| 2 | 产物 SHA256 与预期一致 | ✅ PASS | `0AF54BFF…C9F06C` = expected |
| 3 | 声明指向 0.9.2 | ✅ PASS | `file:D:/FF/project-nav/dsh-external-project-nav-0.9.2.tgz` |
| 4 | bundles 列表仍含该包 | ✅ PASS | `dsh.profile.bundles` |
| 5 | 实体版本 = 0.9.2 | ✅ PASS | 已装版本 = `0.9.2` |
| 6 | 实体含 `core/` | ✅ PASS | 目录存在 |
| 7 | 实体已无 `shared/` | ✅ PASS | 0.9.x 用 core/ |
| 8 | 实体含 `ARCHITECTURE.md` | ✅ PASS | 架构契约随包分发 |
| 9 | 实体不再缺文件 | ✅ PASS | 共 12 个源文件 |
| 10 | **实体与仓库源码逐字节一致** | ✅ PASS | **全部 12 个文件一致（core + host）** |

> 第 10 项是**防「假绿」的关键项**——脚本自带警示（L56）：只比 `core/model.js` 会漏掉改在 `core/legacy.js` 的修复，校验器自己就成了假绿。本次是全量比对。

---

## 🧪 测试覆盖 & CI 状态

| 项目 | 结果 |
|------|------|
| `npm test`（独立复跑） | ✅ **21/21 PASS / 0 FAIL**（host 套件） |
| 提交方声明（全四套） | ✅ **98/98**（core 43 · architecture 22 · concurrency 12 · host 21） |
| 前次失败 `ERR_MODULE_NOT_FOUND: core/boundary.js` | ✅ **已消失**（反证为并发在途态，非清理致因） |
| `git status` | ✅ 干净（仅 2 个新脚本未跟踪，属发布产物） |

---

## 🔄 漂移消解对照（前次发现 → 本次复验）

| 前次发现（18:28） | 本次复验（18:52+） | 状态 |
|---|---|---|
| 仓工作区 0.9.2（未提交）vs HEAD 0.9.1 | HEAD = **0.9.2**（`b07e42d`） | ✅ 已收口 |
| 线上安装副本 0.9.1 | 线上 = **0.9.2** | ✅ 已同步 |
| 安装副本 `host/index.js:33` 仍 import 已删的 `boundary.js` | **无 boundary import**；`core/boundary.js` 在实体中 **ABSENT** | ✅ 已消除 |
| `core/format.js` sha 不一致 | **SAME**（`f226a585`） | ✅ 已一致 |
| `host/index.js` sha 不一致 | **SAME**（`b9033411cf…`） | ✅ 已一致 |
| `npm test` 阻塞 | **21/21 PASS** | ✅ 已解除 |
| 建议「冻结打包」 | 冻结**解除**（0.9.2 已提交并打包） | ✅ 已释放 |

线上 import 冒烟：`OK exports: Config,apply,inject,name`（注意 `governedWorkspaceOf` 已随边界特性退役消失，符合 0.9.2 语义）。

---

## 🚦 Go / No-Go 决策

| 判据 | 结论 |
|------|------|
| 制品指纹 | ✅ Go |
| profile 声明 | ✅ Go |
| 安装实体身份 | ✅ Go |
| 实体↔源码一致性 | ✅ Go（12/12 逐字节） |
| 测试 | ✅ Go（98/98） |
| 工作树状态 | ✅ Go（干净） |

### **决策：🟢 Go**

**唯一剩余动作（人工）**：重启 dsh-web。**agent 禁止自行重启**（脚本 L78 明示）。
**重启后验证**：新会话里工具数应为 **6** 个 —— `nav_graph` / `nav_commit` / `nav_decide` / `nav_node` / `nav_render` / `nav_set`（旧版 11 个）。

**回滚方案**：若重启后异常 —— 用 `package.json.bak-*` 覆盖回去，再 `npm install`（脚本 L82 预设路径）。

---

## ✅ 行动清单（按优先级排序）

| # | 行动 | 负责角色 | 紧急度 | 预期完成 |
|---|------|---------|--------|---------|
| 1 | **重启 dsh-web**（须人工在终端执行，agent 不代劳） | 人 | P0 | 即刻 |
| 2 | 重启后新会话验证工具数 = 6（`nav_graph/commit/decide/node/render/set`） | 人 | P0 | 重启后 |
| 3 | 下一轮清理：0.9.0/0.9.1 的 7 项制品（3 tgz + 4 脚本，均已 superseded 且属 `unregistered`） | 人拍板 → 我执行 | P2 | 重启验证通过后 |
| 4 | 决定隔离区 `.trash-20260911/` + 安全档（约 1.5 MB）是否永久清除 | 人 | P2 | 观察 1–2 天 |

---

## ⚠️ 待完善 / 已知局限

- **模型快照滞后**：`.internal/runtime/arch-model.json` 的 `builtAt = 10:43`、`eventCount = 133`，**早于 0.9.2 提交**（提交新增事件 seq 23/24 用于退役 `PN-F06` 与 `core-boundary`）。该文件是 I3 可弃折叠物，下次治理动作会自动重建，**非缺陷**；但因此 0.9.2 制品尚未出现在 `unregistered` 清单里。
- **`0.9.1` 遗留的定性**：其 7 项制品处于治理模型 `unregistered` 桶（共 3571 项），即「治理可见、但无 feature 节点认领」。这说明它们**从未被登记为交付物**，故清理**不需要走 `nav_node retire` 级联**，直接归档即可 —— 但仍建议**先重启验证再清**，把 0.9.1 留作即时回滚锚点（脚本 L82 的回滚路径依赖 `package.json.bak-*`，与 tgz 无关，故风险低）。
- **本次未执行任何写操作**（除报告与记忆）；`project-nav` 保持只读，未与并发会话的已提交成果发生任何交错。

---

## 📚 数据来源 & 成员产出索引

- **Rex（SRE）**：`verify-0.9.2-install.ps1` 九项全绿、profile 声明与 bundles 校验、重启指引与回滚路径确认。
- **Cody（代码审查师）**：`core/*.js` 10 文件 + `host/index.js` 逐字节 sha1 对照（11/11 SAME）、`boundary.js` 实体缺失确认、`npm test` 复跑 21/21。
- 主理人：漂移消解对照表、`unregistered` 桶定性、Go/No-Go 汇编。
- 前序报告：`cleanup-verify-2026-09-11.md`（漂移首次发现）· `cleanup-leftovers-2026-09-11.md`（清理执行）· `governance-review-2026-09-11.md`（治理审查）· `distill-chain-check-2026-09-11.md`（蒸馏链）

---

> 本报告由工程保障团队 AI 协作生成，关键决策请由人类工程负责人复核。
