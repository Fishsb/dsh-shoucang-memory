# v9 全面对齐 · 修改方案（8 视图片）

> **标准**：以 `deliverables/ui-redesign-v9-2026-09-13.html` 的「优化后」帧（`#newFrame`）为**最终标准**，
> 逐块对齐，不再讨论"原型是否自相矛盾"。
> **依据**：两侧 DOM 同口径实测（`deliverables/v9-compare/skeleton-all.txt`）+ 原型 CSS 精确值（`v9-spec-overview.md`）。
> **出图**：整页长图 `deliverables/v9-compare/full/`。核对时间 2026-09-13 17:43。

---

## 0 · P0 前置（**不修则后续核对全部失真**）

| # | 问题 | 证据 | 修法 |
|---|---|---|---|
| **P0-1** | **夹具索引行格式错误** | 面板记忆库页整页图显示「⚠ 8 条索引行数据格式异常已跳过（期望 \| tag \| subject \| pointer \|）」；画像页 USER/AGENT 卡体内为空 | `scripts/ui-geo-regress.mjs` 的 `FIX`：`indexes[].lines` 由 `new Array(N).fill('[x]')` 改为真实格式 `'\| 架构 \| 部署链路四跳 \| notes/lessons.md §部署 \|'`（各文件给 3–5 条真实感数据） |
| **P0-2** | 探针类名覆盖不全 | 面板记忆库页实测 `card:0`，实际页面有内容（`.sc-idx-*` / `.sc-tabbox` 内） | `_tmp-skeleton.mjs` 的 `sub` 选择器补面板实际类名（`.sc-idx-row` / `.sc-tabbox` / `.sc-fold` / `.sc-stat` / `.sc-legend` …） |
| **P0-3** | 原型底部说明区混进"页面组件" | 整页图里 `.wrap`（异常与边界态 / 施工图纸）与页面本体连在一起 | 出图时给 `.wrap` 加分隔标注，或分两张图（页面本体 / 说明区） |

> P0-1 影响面最大：记忆库、画像两页的**所有列表类核对结论在修好前都不可信**。

---

## 1 · 运行总览 `overview`

**实测**：容器高原型 1328 ↔ 面板 724；两栏 card 原型 **6** ↔ 面板 **2**。

| # | 差异 | v9 标准（原型） | 改法 | 涉及 |
|---|---|---|---|---|
| **1-1** | 缺「**晨起摘要**」卡 | hd（标题 + `.sub`「delta · 剩余 6h 有效」+ src `/memory/overview · delta / weekDiff`）→ bd **3 × `.row`**：`部署链路最后一跳是用户热重载`/`G-20 读方根因…`/`近 7 天深睡新习得 12 条`，右列 `.pill`（`injections 14` / `路径` / `+12`） | 左栏加第 2 张卡；数据取 `d.delta`（`{staleAt, rows[], injections}`）+ `d.weekDiff`（`{deepAdded}`）。**零新端点** | `renderViewOverview` |
| **1-2** | 缺「**最近动态**」卡 | hd（标题 + `.sub`「最近 24 小时」+ right 2 `.pill`）→ `.tl` **4 条**（`.tl-item.ok/.warn/—` + `.tl-t`/`.tl-d`/`.tl-time`） | 左栏加第 3 张卡；事件源待定（见 §9 待拍板），先按「蒸馏完成 / 画像升格 / 容量预警 / 深睡整理」四类占位 | 同上 + 新增 `.sc-tl*` CSS |
| **1-3** | 缺「**判据与重排门**」卡 | hd（标题 + `.sub`「现状已有（L2912）· 仅改归属」+ src `GET /criteria`）→ `.mini` 三列 **5 组**：判据版本 v2.2.0 / 台账行数 1,284 / notes 告警阈值 8,000 / 重排门 `1,204 / 200 · 行数门已达 · 连败门 0/2 · enabled=false` / 库版本 417 提交 + `.bp-note` | 右栏加第 2 张卡，`GET /criteria` 已有全部字段（`version`/`ledger.rows`/`notesWarn`/`rerankGate`/`bankGit.commits`）；**同时从深度睡眠页移除**（原型深睡页无此块，见 §5） | 同上；`renderRunExtras` 拆分 |
| **1-4** | 缺「**快捷操作**」卡 | hd（**仅标题**）→ bd flex-wrap **4 个默认 `.btn`**：测试嵌入连通 / 根目录引导 / 成熟度扫描 / 账本对账 | 右栏加第 3 张卡。前三/四个动作端点：`POST /embed/test` ✓ · `POST /root/bootstrap` ✓ · `POST /reconcile` ✓ · **成熟度扫描无端点**（见 §9 待拍板） | 同上 |
| **1-5** | **系统状态卡被误删 3 行** | 原型该卡 **4 行**（当前根目录 / MCL 认知环 / 注入统计 / 嵌入服务），各带右侧 `.pill` | 恢复 3 行：`MCL 认知环 · 熟悉度 / 慢通道`（`/mcl/status`）、`注入统计 · N 次 / 最近时间 / root`（`/inject/stats`）、`嵌入服务 · provider/model/可达`（`/vector/status2`）。上一轮删除依据是**方案文档正文的说法**，与 DOM 不符 | `renderViewOverview` 的 `ovSys` |
| **1-6** | badges 文案 | 全部为「**标签 · 状态词**」：`蒸馏 · 2 分钟前` / `向量 fusion` / `候选 9` / `记忆库 · 正常` / `认知环 快 23/慢 7` / `判据台账 · 已就绪` / `库版本 · 已启用` | 面板改为：蒸馏→`蒸馏 · <相对时间>`；向量**保留 provider 英文标识**（`fusion`/`lexical`，不翻译）；判据台账→`判据台账 · 已就绪`；库版本→`库版本 · 已启用`；记忆库→`记忆库 · 正常/水位偏高`（阈值 60/85 保留） | `renderViewOverview` 回填段 + `Derive.vecLabel` |
| **1-7** | 告警条结构 | 三角图标(svg 15px) + `<b>N 项待处理</b> —— <描述>` + 「前往处理」链接（色 `--brand`） | 面板改为：加图标、前缀 `<b>N 项待处理</b> ——`、尾部加跳转链接（指向待处理所在页） | `setAlert` |
| **1-8** | 操作卡端点 chip | `.act .f` 内 `.src` chip（等宽 + 边框 + `--panel2` 底 + 10.5px） | 面板端点文本由 `sc-mem-sub muted`（灰字）改 `.sc-src` chip | `opCard` |
| **1-9** | 页头 | 右侧 = `刷新`（`.btn.ghost`）+ `.proto-note`「蒸馏执行位：下方操作卡（本页仅一处）」 | 加 `.proto-note` 注记；描述文案按原型 | `UI.pageHead` 调用 |
| **1-10** | 几何 | 操作卡组 156px（面板 138）；KPI 卡 220×110 | 随 1-8 / 1-9 改动后复核 | — |

> **已对齐（不动）**：徽章 7 枚数量与顺序 · KPI 4 张**且四卡进度条共线** · 操作卡 3 张四要素齐 · 两栏 `1.35fr:1fr` · 告警条位置。

---

## 2 · 记忆库 `memory`

**实测**：原型 `div.card` 914×661（内含 **kpi:3 / card:6 / row:18 / pill:18 / bar:3 / btn:13 / src:5 / alert:2**）↔ 面板 `div.sc-card` 946×516（探针 card:0，见 P0-1/P0-2）。

| # | 差异 | v9 标准 | 改法 |
|---|---|---|---|
| **2-1** | 容量区应为 **3 KPI** | `MEMORY.md 容量 62% / 3,100-5,000 字符 · 86 行`（有 bar）、`notes/ 笔记 148 个文件 · 6.2 MB · 无容量门`（`k-bar.na` 虚线空槽）、`pending 候选 9 条待裁决 · 无容量门`（同 na） | 面板现已 3 列 ✓ 需补 **`.k-bar.na` 空槽形态**（保持槽位一致） |
| **2-2** | **6 张卡** | 原型 5 个 Tab 内的卡：知识索引（MEMORY.md 卡）/ 候选区（pending 卡 + info alert）/ 笔记（notes 七类卡 + 最近笔记卡）/ 归档区（归档卡 + ok alert）/ 统计（`.mini` 卡） | 按原型逐 Tab 补卡；补 **2 条 alert**（候选区 `info`、归档区 `ok`） |
| **2-3** | 索引行 **18 行**（含 pill） | 行结构：左 `.pill`（语义色）+ 中间 title/desc + 右 `.pill` + 条数 | 面板 `renderIndexRows` 需输出同构行（**依赖 P0-1 夹具修复才能验证**） |
| **2-4** | 5 个 Tab 文案 | `知识索引 86` / `候选区 9` / `笔记 7 类` / `归档区 4` / `统计`（**无计数**） | 面板现为 4 Tab（知识索引/候选区/笔记/统计）；补「**归档区**」Tab 与各自计数 |
| **2-5** | 5 枚 `.src` chip | 每个 Tab 卡头带端点 chip（`/memory/sections?rel=&root=` 等） | 补 |

> 注：上一轮曾删掉「状态徽章行」「运行态 Tab」——**原型记忆库页确无这两块，删除正确**，保留。

---

## 3 · 画像 `persona`

**实测**：块数 6 ↔ 6 **结构一致** ✓。差异在形态与内容。

| # | 差异 | v9 标准 | 改法 |
|---|---|---|---|
| **3-1** | 构成块容器 | 原型 `div.stat2` **914×148**，内含 2 × `.stat-box`，每块带 `.rule` 行 + 前置语义 `.dot` | 面板 `div.sc-mem-grid` 936×123（少 25px、无 dot）。补 `.dot` 与间距，对齐 148 |
| **3-2** | USER/AGENT 卡高 | 原型 **282 / 254**（3 行列表 + pill） | 面板 138 / 132（列表空 ⇒ P0-1 修好后自然对齐；仍矮则查行高） |
| **3-3** | 卡头结构 | 原型：`USER.md · 用户画像` + `.sub 24 条` + right **`压缩画像` 按钮**（AGENT 卡**无**按钮） | 面板 `USER` / `AGENT`（缺 `.md · 画像` 后缀）+ right 有 src chip。改标题文案；AGENT 卡确认无按钮 ✓ |
| **3-4** | 行结构 | 原型行：左 `.pill`（语义色）+ title/desc + right **成熟度数值**（`0.81` / `0.76`） | 面板用 `.sc-idx-*`（无成熟度数值列）⇒ 需补 maturity 数值列 |
| **3-5** | 页脚注 | 原型 `.bp-note` 41px（说明 v2 移除依据不成立） | 面板 `.sc-note` 20px（文案不同）。按原型改文案与行高 |
| **3-6** | 成熟度分布卡高 | 原型 189 | 面板 225（柱区 88px + 图注两行）⇒ 复核柱区高度与图注行数 |

---

## 4 · 插件集合 `suite`

**实测**：原型 `div.grid2` 200（**4 张 pcard**）+ `div.card` 274（矩阵表 `pill:5`）↔ 面板 `div.sc-pgrid` 81（**1 张**，夹具 `members:[]`）+ `div.sc-card` 221（`pill:3`）。

| # | 差异 | v9 标准 | 改法 |
|---|---|---|---|
| **4-1** | 成员卡 **3 张 + 添加卡** | `managing-memory`（记忆技能本体 · 目标库 / **5 工具** / 已装配）· `shoucang-core`（守藏核心 · 蒸馏器 / 3 工具）· `scheduler`（调度 · 定时蒸馏 / 2 工具）· 虚线「添加目标库」 | 夹具 `members` 补 3 条（P0 类）；真机为单成员 ⇒ **按 v9 标准补"工具数"字段**（见 §9 待拍板） |
| **4-2** | 卡头结构 | `.ph`：26px 图标砖（`--brand-soft` 底 + `--brand` 色）+ `<b>包名</b>`；下行 11.5px 灰字 role；`.pmeta`：左 `N 工具` / 右 `已装配` | 面板 `.sc-pcard` 需补图标砖 + 工具数 |
| **4-3** | 装配矩阵表 | 5 行（MEMORY/USER/AGENT/notes//archive/）× **5 列**（目标库/容量/已用/装配内容/状态），状态列 `.pill.ok` | 面板 `pill:3`（列不同）。按原型列结构改表头与单元格 |

---

## 5 · 深度睡眠 `sleep`

**实测**：原型 **6 块 / 1531px** ↔ 面板 **15 块 / 705px** —— **面板比原型多 9 块、少 3 块**。

**原型 6 块**（必须一一对应）：
1. `card` **状态机** 183（含 `.sm` 三节点 + `.cap` 睡眠水位 `1,240 / 3,000 seq`）
2. `card` **睡眠状态分布** 140（`.dist` 分段条 + `.legend2` 五段 + right `立即进入深睡` 按钮 + src）
3. `card` **本轮产出回执** 296（`.kpis` **7 张**：新增原则/替换原则/画像更新 + 指针更新/树操作/归档/保留）
4. `div`（两栏）**319**：左「下轮材料预估」（`.row` 3 行 + right `<b>N 条</b>`）· 右「待执行结构操作」（`.row` 3 行 + pill `merge`/`split`/`archive` + 执行按钮）
5. `card` **最近会话** 277（`.row` 3 行 + pill 状态 + `查看` 按钮）
6. `card` **睡眠期自检** 161（`.code` 代码块 + right `运行自检` 按钮）

**面板多出的 9 块（以 v9 为标准 ⇒ 需移除或迁移）**：

| 面板块 | 处置 |
|---|---|
| `sc-card` 判据与对账（v2 · ADR-122） | **迁到总览页**（原型总览有「判据与重排门」） |
| `sc-card` 账本对账与产出健康（v2.1 M3） | **移除**（原型深睡页无此块） |
| `sc-card` 认知环（MCL · 快/慢双通道） | **移除**（内容已在总览徽章「认知环 快/慢」+ 系统状态卡） |
| `sc-ds-badges` 会话徽章 | **移除**（原型把会话信息放在「最近会话」卡内） |
| `sc-ds-timing` 已停滞/下次入睡/上次入睡 | **移除**（原型在状态机的 `.cap` 睡眠水位里表达） |
| `sc-ds-alert` 卡住告警 | 保留（**这是真功能**；原型未画但属异常态，见 §9 待拍板） |
| `sc-h3 控制` + `sc-ds-ctl`（立即归纳/暂停） | **合并进「睡眠状态分布」卡右侧**（原型该卡有 `立即进入深睡` 按钮） |
| `sc-h3 阈值` + `sc-more-btn` + `sc-more-body` | **移除或收进设置页**（原型深睡页无阈值面板） |
| `sc-btn 立即跑一次自检` | 已在「睡眠期自检」卡内 ✓ |

**面板缺失的 3 块（需补）**：本轮产出回执（7 KPI 版）· 下轮材料预估 · 待执行结构操作 · 最近会话（现有"会话明细"形态不同，需按原型改）。

---

## 6 · 运行观测 `observe`

**实测**：原型 **6 块**（4 KPI / 执行进度 card / `tabs` 3 枚 / 3 × `pane`）↔ 面板 **3 块**（4 KPI / 折叠执行进度 / `sc-tabbox`）。

| # | 差异 | v9 标准 | 改法 |
|---|---|---|---|
| **6-1** | KPI **不应有 dot 与进度条** | 原型 `div.kpis` 914×**104**，`kpi:4`（**无 dot、无 bar**）：请求总数 2,457 / 成功率 99.2% / 平均耗时 86ms（子行 P95）/ 错误 10 | 面板 `dot:4 bar:4`（110 高）⇒ **去掉 dot 与 bar**，对齐 104 |
| **6-2** | 执行进度是**卡**不是折叠块 | 原型 `div.card` 146，hd「执行进度」+ sub「现状已有（L2610 UI.progress）· 复用提升」+ src `/cognition/report` | 面板是 `sc-fold.open`（89）⇒ 改 `UI.card` 形态 |
| **6-3** | 分组是 **`tabs`** 不是 tabbox | 原型 `div.tabs` 34 + 3 × `div.pane`（pane.on 163） | 面板 `sc-tabbox` 237（4 Tab）——**结构等价**，需核对 Tab 文案（原型 3 枚：调用日志 / 错误定位 10 / 运维操作）与面板 4 枚（多了「关键指标」）的取舍 |
| **6-4** | 日志行结构 | pane.on 内：`✓ GET /memory/overview 42ms 200` 形态（带 ✓/耗时/状态码） | 面板日志行 `17:43:36 info GET /g…`（无状态码列）⇒ 按原型补 |

---

## 7 · 参数 `param`

**实测**：原型 **5 块**（`tabs` 4 枚 + **4 × `pane`，每个 pane 内 1 张 `card`**）↔ 面板 **1 块**（`sc-tabbox`，内含 `row:2 / dot:2 / badge:2 / btn:18`）。

| # | 差异 | v9 标准 | 改法 |
|---|---|---|---|
| **7-1** | **每组配置用 card 容器** | 原型每个 pane = 一张 `div.card`（hd 标题 + bd 若干 `.setrow`） | 面板是裸 `.sc-setrow` 平铺（18 个按钮）⇒ **按组包 card**，每组给独立标题 |
| **7-2** | 配置行结构 | 原型 `.setrow`：`.t` 标题 13px/520 + `.d` 描述 11.5px `--faint`（max-width 520px） | 面板已有 `.sc-setrow`。核对字号/宽度 |
| **7-3** | alert 提示 | 原型 pane③/④ 各含 1 条 `alert`（「改动写入 scheduler.json，需重载插件后生效」） | 面板无 alert ⇒ 按原型补 |

---

## 8 · 设置 `settings`

**实测**：原型 **3 块**（`tabs` 2 枚 + 2 × `pane`）↔ 面板 **1 块**（`sc-tabbox` 1180）。

| # | 差异 | v9 标准 | 改法 |
|---|---|---|---|
| **8-1** | 「界面偏好」pane | 1052px，`card:2` + `row:3` + `pill:3` + `btn:6` | 面板 tabbox 内含 2 Tab ✓；按原型补 card 包裹与 pill |
| **8-2** | 「高级」pane | `card:5` + `row:6` + `pill:5` + `btn:4` + `src:4` + `alert:1` | 面板高级 Tab 需逐卡核对（配置原文 / 根目录 / 最近改动 / 注入预览 / 行级编辑） |
| **8-3** | 快捷键卡 | 原型 pane① 内有**快捷键**卡 | 面板需确认存在 |

---

## 9 · 待拍板（3 项，无法从原型推出）

| # | 事项 | 选项 |
|---|---|---|
| **9-1** | 「最近动态」事件口径（原型未标端点） | ① `/cognition/report`（深睡/归档事件）② `distillStats` + `growth`（蒸馏/升格）③ 面板本地日志 Store |
| **9-2** | 插件集合「**N 工具**」（插件未上报工具数） | ① 后端在 `/suite` 增 `tools` 字段（需定"工具"采集口径）② 不显示该列 |
| **9-3** | 总览「**成熟度扫描**」按钮（插件无端点） | ① 新增 `POST /maturation/scan` 触发脚本 ② 改只读展示（上次扫描时间 + 分档）③ 去掉 |

---

## 10 · 施工顺序

| 批 | 内容 | 风险 | 验收 |
|---|---|---|---|
| **P0** | 修夹具索引行格式（P0-1）· 扩探针类名（P0-2） | 无 | 记忆库/画像页整页图能渲染出列表 |
| **P1** | 总览 1-1/1-5/1-6/1-7/1-8/1-9（晨起摘要 + 恢复 3 行 + 文案 + alert + chip） | 低 | 整页图复核 + `check-panel-contract` |
| **P2** | 总览 1-3/1-4（判据卡迁移 + 快捷操作）· 观测 6-1/6-2 | 低–中 | 同上 |
| **P3** | 深睡页 5-*（移除 9 块 / 补 4 块） | **中–高** | 该页功能回归（触发/暂停/阈值入口不丢） |
| **P4** | 记忆库 2-* / 画像 3-* / 集合 4-* / 参数 7-* / 设置 8-* | 中 | 每页一轮"核对→改→整页出图→契约" |
| **P5** | 待拍板 3 项落地 | 视选项 | — |

**每批通用验收**（AGENTS.md 规则 7）：`typecheck` + `build` + `check-runner` 全绿 + **整页出图复核** + `check-panel-contract` + 部署差分。

---

## 11 · 第十二轮进度（2026-09-13 晚 · 逐页结构对齐）

**本轮先把验收基建固化**（此前「整页长图」与「DOM 骨架实测」都是一次性脚本、随清理出仓 ⇒ 每页只看得到 860px 首屏 ≈ 15%，正是第十一轮误判「总览只有 2 张卡」的根因）：
`ui-geo-regress --full-shots <dir>` 出「面板 + 原型」各 8 张整页长图（先实测高度再截图，不放空白）；`--skeleton <dir>` 出两侧块树 txt（同一套选择器 + 路径编号 + 类计数），供程序化 diff。
三个坑：截图输出路径必须**绝对**（相对路径会静默落到别处）· 目标目录须先建 · 骨架递归深度要 ≥8（卡在 `wa-tab-panel` 之下时会被截断，误判成"结构缺失"）。

| 页 | 状态 | 本轮改动 / 证据 |
|---|---|---|
| 运行总览 | ✅ 第十一轮 | `w8/proto-overview.png` ↔ `panel-overview.png` |
| 设置 | ✅ 本轮 | 6 个折叠组 → **一张卡平铺** + 「快捷键」卡（键位 pill 中性色）+ 「恢复默认」动作行；页头「设置」+「导出快照」；补**导航分组显示 / 页脚健康条**两枚真实开关 |
| 参数 | ✅ 本轮 | 4 个 pane 各**包一张卡**（原型 `div.card.plain`） |
| 运行观测 | ✅ 本轮 | KPI 去 dot/bar（104 高）；执行进度折叠块 → **卡** + 路由 chip；日志行「图标 + 方法 + 端点 + 耗时 + 状态码」（真实值来自 `api()` 写入 ctx） |
| 插件集合 | ◑ 部分 | 夹具补 3 条成员（此前 `members:[]` ⇒ 网格恒只有添加卡，形态无法核对）；「N 工具」仍无数据源 |
| 记忆库 | ◑ 部分 | 容量区排版与标准 KPI **收敛为同一组值**（clamp 值字号 + min-height 34 + 条沉底共线）；归档区 Tab 回填计数；**pane → 卡**层级未做 |
| 深度睡眠 | ✅ 本轮（大改） | **14 块 → 6 块**：移除判据/认知环（总览已有）· 账本对账（迁观测页「关键指标」）· 会话徽章与计时（信息已在分布卡/状态机）· 控制小节（触发并入分布卡头执行位）· 阈值小节（迁设置页 › 高级）；补**本轮产出回执（7 KPI）**·**下轮材料预估（3 行）**·**最近会话卡** |
| 画像 | ◑ 部分 | 卡头文案「USER.md · 用户画像」+ sub 去多余前缀；成熟度数值列未做（后端无该字段） |

**仍未对齐（下一轮接手点）**：
1. 记忆库页各 Tab 的「pane → 卡」层级（原型 pane 内是一张卡 + 卡头 src chip，面板仍是裸小节）。
2. 画像页指针行的**成熟度数值列**（`/memory/overview` 未上报逐行 A 值）。
3. 设置 / 参数页行高（原型 `.setrow` 65，面板 `.setting-item` ~94）。
4. 深睡页「待执行结构操作」块（面板无 merge/split/archive 候选数据源）与「自检卡 ↔ 最近会话卡」顺序（原型自检在末）。
5. 插件集合「N 工具」字段（§9-2 待拍板的延续）。
