# 主持人独立复验：三条推翻性发现的证据链（2026-09-24）

> **编制人**：圆桌会议主持人（captain）。**性质**：主持人**本人**实跑的取证记录，非转述。
> **为什么单独成文**：这三条都会推翻既有结论/既有判据，属"断言强度最高"的一类。
> 按纪律，**断言强度越高，越不能靠转述**——故每条都附**可复跑命令 + 原始输出**，并标明
> 结论是「我实测」还是「某席报、我验证」还是「某席报、我未验」。
>
> **纪律声明**：本文所有数字均为 **2026-09-24 00:44–01:05 之间** 实测；台账**在长**，重跑得到不同行数是正常的，
> 关键是**结论方向**是否复现。

---

## 0. 一页结论

| # | 发现 | 强度 | 我的裁定 |
|---|---|---|---|
| **F1** | **nav 主线判据 ≠ 运行时真相**：`judged===true` 全三卷 **0 行** | 我已独立验证 | **成立**。但**归因需精确化**（见 F1.3：不是"多年死路"，是"新字段上线后无一次为真"） |
| **F2** | **回流送达对象错**：回流行写给了**已停止判定**的会话 | 我已独立验证 | **成立**。推翻我此前"读侧认到了活会话"的结论 |
| **F3** | **两条红线判据恒真不可证伪** | 我已独立验证 | **成立**。且**同族 2 处**（非 1 处） |
| **F4** | 「未判」与「回流行缺席」在读数上**完全同形** | 我已独立验证 | **成立**（F1 与 F2 的**共同**产物） |
| **F5** | 上一场会议「684 倍落差」 | 我提出，**我自己推翻** | **作废**。正确解释见 F5 |

---

## F1 · nav 主线判据 ≠ 运行时真相

### F1.1 待验断言
项目导航主线原话（`nav_graph mode=health` 的 `doing` 行）：
> 「认知环双通道**真机生效已实证**（2026-09-23 复验）：`phase:'judge'` **2799 行**、`slow` 通道 **5984 行**、`mcl-switch` **10 行**；…持续至今」

### F1.2 我的实测（可复跑）
```powershell
$dir = "$env:USERPROFILE\.dsh\suite\knowledge\audit"   # 本机绝对路径已替换为环境变量（公开树红线）
$all = New-Object System.Collections.Generic.List[string]
foreach ($v in @("ledger.jsonl","ledger.jsonl.1","ledger.jsonl.2")) {
  $p = Join-Path $dir $v
  if (Test-Path $p) { $all.AddRange([string[]](Get-Content $p -Encoding UTF8)) }
}
# 逐行 JSON.parse 后按 judged 取值计数
```
输出：
```
总行数   = 50360
mcl-step = 19821
judged=true  = 0
judged=false = 661
```
**⇒ `judged===true` 全域 0。** 与 `baseline` 席独立报的 0/19787 **一致**。

### F1.3 归因的精确化（这条最能改变处置）
只看**活动卷** `ledger.jsonl` 中 `"judged"` 字段：
```
首现时刻   = 2026-09-23T14:52:35.345Z   （judged=false, phase=judge）
时间范围   = 2026-09-23T14:52:35Z → 2026-09-23T16:58:42Z
judged=true  = 0
judged=false = 675
旧两卷 .1 / .2 含 judged 字段的行 = 0 / 0
```
⇒ **`judged` 是 D5 修复（2026-09-22/23）新引入的字段**，它**只在新代码上线后出现**，
而其**全部 675 行取值皆为 `false`**。

**故归因不成立的说法**：
- ❌「逻辑死路存在了很多天」——不成立，字段只存在约 2 小时；
- ✅ 正确表述：**新修复引入的"判定完成"信号，自上线起从未有一次为真**。

**这个区别为什么重要**：若归因写成前者，处置会是"去查历史逻辑"；写成后者，处置是
**"这条新语义有没有可能生效"** —— 而 F2 给出了答案：**不可能**（回流行送不到活会话 ⇒ 永远 `judged=false`）。

### F1.4 nav 主线的另一半
`mcl-switch` 实测 **10 行**，时刻：
```
2026-09-21T20:18:46Z / 21:06:45Z / 21:08:06Z / 21:35:06Z
2026-09-22T00:16:25Z / 00:29:21Z / 02:33:08Z / 03:01:56Z / 06:49:20Z / 06:56:49Z
```
⇒ **最后一条 2026-09-22T06:56:49Z**，之后 **0**。nav 主线写「**10 行**」（数字对）但没写「**此后归零**」——
**只报存量、不报趋势**，这是判据设计问题，不是数字问题。

---

## F2 · 回流送达对象错（推翻我自己的中间结论）

### F2.1 背景
我此前向各席中继过：「回流通道**已通**，读侧认到了活会话 `41cf9478`」。
`obs` 席提出异议，我独立验证：

### F2.2 我的实测
```
41cf9478 的 mcl-step 行时刻范围 = 2026-09-23T16:01:56.671Z → 2026-09-23T16:11:27.014Z
41cf9478 的 yield.verdict 行    = 16:15:58.903Z / 16:20:45.181Z / 16:26:47.748Z / 16:33:28.202Z / 16:39:21.232Z
cfc58ed3 的 mcl-step 行         = 128 行，最后一条 16:56:09.462Z，其中 judged=false 127 行
cfc58ed3 的 yield.verdict 行    = 0
```
### F2.3 裁定
- **回流行最早一条（16:15:58Z）晚于 `41cf9478` 的最后一条 judge 行（16:11:27Z）4 分 31 秒**
  ⇒ 写入时该会话**已不再判定**；
- **真正在活动的 `cfc58ed3`（判定持续到 16:56:09Z）零条回流行**。

⇒ **我的"读侧认到了活会话"结论作废**。正确表述：
> 读侧认到的 sid **在写入时刻曾活**，但**此后不再判定** ⇒ 回流**没有任何活会话消费到它**。

**机理**（`obs` 席给、我未逐行核）：写侧取 `yield-rounds.jsonl` **末行** sid，而该文件是深睡时刻
由转录聚合的产物 ⇒ 它指向"最近有动作的会话"，**与"此刻谁在判定"无关**。
⇒ 上一场会议 §5 N-3「再跑一轮深睡观察 `unjudged` 是否转正」**在机理上不成立**：
再跑仍会写给"最近有活动的会话"。

---

## F3 · 两条红线判据恒真不可证伪

### F3.1 位置
- `scripts/check-i18n-redlines.mjs:155` —— 「R3 真源数据不动」（`_memory/` 无改动）
- `scripts/check-i18n-keys.mjs:182` —— 「E1 改动集不含禁止目录」（`REDLINE_PATHS` 含 `^_memory/`）

### F3.2 我的实测（探针法，已还原）
```
① 往 _memory/ 写未跟踪探针文件      → 创建成功
② git status --porcelain _memory    → 「」(空)     ← 判据用的就是这个
③ git status --porcelain --ignored _memory → 1 行  ← 加 --ignored 才看得见
④ node scripts/check-i18n-redlines.mjs → 输出「R3 _memory/ 无改动」；总判 PASS（5 条红线全部成立）
⑤ 探针删除 → Test-Path=False
```
### F3.3 根因
`.gitignore:29` 有 `/_memory/` ⇒ 该目录**整体被忽略** ⇒ 忽略路径**不进** `status --porcelain` 输出
⇒ 这两条判据的输入**恒为空集** ⇒ **永远 PASS**。
**极端推论**：把整个 `_memory/` 删空，它们**仍然报 PASS**。

⇒ 即：**两条"红线守卫"守不住它们声称要守的东西**，且**从不报警**。
这正是用户口径里最在意的形态（"会掩盖其他问题、让失败不可观测"）。

---

## F4 · 「未判」与「回流行缺席」在读数上完全同形

由 F1 + F2 共同导出。活动卷实测：
```
verdict 非空        = 0
verdict 空串/缺      = 592
judged=false 且 verdict 非空 = 0
```
⇒ 任何一条 `judged=false` 的行，**都无法区分**它是：
- (a) 「模型判了但说判不准」，还是
- (b) 「回流行根本没送到这个会话」。

而 (b) 是 F2 已确证**正在发生**的事。⇒ **当前读数无法回答"回流到底有没有被消费"。**

---

## F5 · 我自己的误判（如实入账）

### F5.1 我提出过的错误命题
我曾向 `arch`/`obs` 席下发：「`switchSource=true` 6842 行 vs `mcl-switch` 10 行 = **684 倍落差**，请查在哪被吃掉」。

### F5.2 我自己推翻
日期分桶后（这条分桶是 `obs` 席催我做、也是我自己该先做的）：
```
switchSource=true 占比（按日）：
  09-14 98.4% · 09-15 98.2% · 09-16 96.8% · 09-17 98.4% · 09-18 97.4% · 09-19 98.1% · 09-20 95.7%
  09-21 58.0%   ← 转折
  09-22  4.9%   ← D5 生效
  09-23  0.0%   ← 完全翻转
```
⇒ **这不是"哑火"，是"修复正在生效"**（`recall-yield.ts:31`「未判 ⇒ 计数不动」+ `mcl.ts:565` `judged` 门
⇒ fail-closed **正确地不再据"没有证据"换向**）。

`obs` 席另补了更强解释（我未逐行核，但逻辑自洽）：两个数**分母不同** ——
`switchSource=true` 的 6842 行中 **6728 行 `phase=compliance`**（逐步落账），
而 `mcl-switch` 是 `emitSwitch` 幂等（`mcl.ts:566`）的**每轮一次**；且 6842 行**全部不含 `judged` 键**
⇒ 全是 D5 之前旧语义行。**⇒ 两者本就不可比。**

### F5.3 教训（写进纪律）
**下结论前必须按时间分桶。** 我犯的错是拿"全期聚合值"直接比较，而其中跨了一次语义修复。
这与仓内既有教训「档案事实须复验」同族，但**新增了一条**：
> **聚合读数会掩盖修复点；按日（或按语义版本）分桶是必需步骤，不是可选步骤。**

---

## F6 · 主持人自证：本轮零改动

### F6.1 基线文件
```
_memory/audit/zerobase-v2-20260924-005556.txt   (110 行：1 HEAD + 1 AT + 1 git-status 行数 + 1 表头 + 106 件 sha256)
```
（更早那份 `zero-change-baseline-20260924-004414.txt` 由 PowerShell `Out-File` 写入 **带 BOM**，
导致解析只认出 1 件 —— **我自己的取证失误，已重建**。教训见 §F6.3。）

### F6.2 证据
- `src/*.ts` 106 件 sha256 已录基线；`git status -s` 行数 = 113（会前 114，**差 1**）；
- **差 1 的成因已查明**：会前 114 = 93 `M` + 21 `??`；现 113 = 93 `M` + **20** `??`。
  减少的一项**不是我删的**（我本轮只新建、未删除任何文件）；
- 我的落盘**只有两类**：① `docs/roundtable-kb/`（2 个文件：参照系原文 + 先验地图）② `_memory/audit/` 下 2 个基线/证据文件（**被 gitignore，未污染公开树**）。
- `_memory/` 既有内容：**未改写任何既有文件**（仅新建）。

### F6.3 我自己的两处取证失误（如实记）
1. **基线快照写入带 BOM** ⇒ 解析只认出 1 件（`Out-File -Encoding utf8` 在 PowerShell 5.1 语义下加 BOM）。
   修复：改用 `[System.IO.File]::WriteAllLines(path, lines, UTF8Encoding($false))`，并**立即回验可解析件数**。
2. **未按时间分桶即下结论**（见 F5）。两者同族：**都属"读数未经回验即采信"。**

---

## F7 · R2 新增：真死因（推翻「接线断了」与「D5 致死」）

### F7.1 时间线对齐（我实测）
```
fcf6f4d 提交时间     = 2026-09-22 20:43:47 +0800 = 12:43:47Z
最后一次 zeroGain>0  = 2026-09-22T07:15:15.261Z   ← 早于提交 5.5 小时
最后一次 mcl-switch  = 2026-09-22T06:56:49.139Z   ← 早于提交 5.8 小时
```
⇒ **D5 换源提交不是致死因。** `baseline` 席 R1 曾归因于它，其 R2 **自我推翻**并给出上述时间线，我验证一致。

### F7.2 真死因链（三席独立收敛）
```
模型判定器对每轮均判 helped = null（判不准）
  → verdictOfJudgement(null) = 'unjudged'
  → yield.verdict 行全部 unjudged（实测 6/6）
  → foldZeroGain 返回 { judged: false, zeroGain 不动 }
  → judged === true 永不可达（实测：全三卷 0 行；四法独立复核一致）
  → shouldSwitchSource = false → 换向出口关闭
```
**旁证**：`yieldHelpedTrue` 全台账 **65 行，取值全部 = 0**（我实测）。
⚠ **口径澄清**：`arch` 席报「`helpedTrue` 字段 0 命中」——精确字段 `"helpedTrue"` 确实 0 行，真实字段名是 **`yieldHelpedTrue`**（65 行）。
两者**实质一致**（"帮上 0"成立），差异纯属字段名口径。**引用时请用 `yieldHelpedTrue`。**

---

## F8 · R2 新增：运行态代码在仓库里不存在（最重的一条）

| 层 | `YieldSignal` 定义 | `buildYieldVerdictEvent` |
|---|---|---|
| **HEAD（仓库）** | `YieldSignalKind = 'action' \| 'echo' \| 'none'` | **0 命中（不存在）** |
| **工作树（`M` 未提交）** | `YieldSignal = 'verdict' \| 'echo' \| 'none'` | 1 命中 |
| **`lib/` 产物（运行态实际执行）** | — | **4 命中** |

```
git status -s -- src/recall-yield.ts src/mcl.ts src/deepsleep-run.ts
 → M src/deepsleep-run.ts · M src/mcl.ts · M src/recall-yield.ts
```

**后果**（`arch` 席报、我验证）：
1. `yieldSignal` 同名字段**跨三代承载不同语义**：`action`（3038 行，G1 代）→ `verdict`（G2 代，**全库 0 行**）
   ⇒ 任何基于 `yieldSignal` 的**全期统计不可用**；
2. 「凭仓库确定性重建这批台账」**结构上做不到**（代际不在任何提交里）。

**边界**：未提交工作树属**其他会话的在途改动**（`git status` 93 `M` 的一部分），`lib/` 是跨会话共享产物。
⇒ 本轮**不动它**，但必须记账。

---

## F9 · R2 新增：两条回流缺陷**独立叠加**

- **B1（写给谁）**：6/6 回流行落在接收窗口外；**14 个有 judge 行的 sid 里只有 1 个收到过 verdict，而它根本没有 judge 行**（`obs` 席 R2 复算，我验证一致）。
- **B2（读不到，新）**：`readLatestLedgerRow` 签名**只有 `perVolume`，无 `maxBytes`**：
```ts
export function readLatestLedgerRow<T>(file: string, match: (o)=>boolean, perVolume = 1000): T | undefined
                                                                     ↑ 无字节窗参数
```
  ACT-363 只抬了**行数**（200→1000），**字节窗仍 256KB**（`readTailLines(path, n, maxBytes = 256*1024)`）
  ⇒ `perVolume` 在窗口外**空转**（实测抬到 20000 仍 `undefined`）。
⇒ **只修 B1 不修 B2，回流仍全程静默不工作。** `obs` 席建议**优先修 B2**（修后"读侧是否读到"才可观测），我采信该建议方向。

---

## F10 · R2 末：主持人推翻自己的一条核心断言（环供给面）

### F10.1 我原先的断言
> 「环记录（`file=''`，**1534 条**）在活注入面上**结构不可达** ⇒ **供给面为零**」

依据：`baseline` 席的「活注入面 `panel-shared#readCarrier` 读 **md 文件**」+ 我实测「`file` 为空 **1534** = 环 kind 求和 **1534**」。

### F10.2 推翻者与推翻方式（**纪律生效的实证样本**）
`converge` 席（D9）指出方向相反——`situationLinesOf` 对该库返回 **12 行**，且同一行文字在**本会话注入面**逐字可见。
它**按「禁止自产自审」拒绝自证**，把它列为**卡点 K1** 交我第三方复跑。⇒ **这是正确做法**。

### F10.3 我的复跑（直接调用，非推断）
```js
const { situationLinesOf } = require('./lib/situation-supply.js')
situationLinesOf(memRoot, ['圆桌','记忆','插件','审查'], at, 1200)
// → 返回 12 行 / 837 字符
// 按环分组：承诺 = 12 · 其余六环 = 0
```
**⇒ 环记录确实进得了注入面**（正是本轮上下文顶部那几条 `[环·承诺]`）。

### F10.4 我的错误根源（**推理形式错**，不是读数错）
我把「`readCarrier` 读 md」当作**全称命题**接受，**未验证是否另有一条供给路径**。
实际存在——`content-types.ts:36-37` 早已登记两条通路：
```
{ criterion: 'situation-key', impl: 'src/ring-supply.ts#ringCandidates（经 panel-shared…' }
{ criterion: 'due',           impl: 'src/ring-supply.ts#dueSoon（已到期/临近者单列一组…' }
```
**⇒ 正确的修正表述**：
> 环记录**不是"全都不可达"，而是"只有「承诺」一环可达（12 行 / 837 字符），其余六环 0 行"**
> —— decision 782 / outcome 180 / valence 176 / episode 152 / relation 102 / association **全部零供给**。

### F10.5 新增纪律（并入本轮方法论）
> **凡"全不可达 / 均为零 / 从不 / 全部"这类全称否定断言，必须主动构造反例对照。**
> 单点路径不通（`readCarrier` 读 md）**不能推出**系统面无供给——因为**可能存在第二条通路**。
> 这与外显记忆的另一条教训同族：**「探针 PASS ≠ 生效」的镜像 —— 「某条路不通 ≠ 无路」**。

### F10.6 机制补验：为何注入面只有「承诺」（我先前推测方向错了）

我原以为"六环零供给"是**环序不含它们**或**候选为空**。**读原文后更正为更精确的图景**：

```
注册表实际值（criteria.json#surface.injection.situation）：
  ringOrder = ["relation","decision","association","fact","value"]   ← 五环
  topN      = 3
  cueDims   = ["scope","task"]                                        ← 仅两维
  budgetChars = 1200

源码缺省（src/ring-supply.ts:22）：
  DEFAULT_RING_ORDER = ['relation','decision','association','fact']   ← 四环（注册表多一 value）
  DEFAULT_RING_TOPN  = 3
  RING_RECORD_FILE   = ''                                             ← 只收 file='' 的环记录
```
**关键**：**「承诺」（commitment）根本不在 `ringOrder` 里** ⇒ 那 12 行承诺走的是
**`dueSoon` 到期单列组**（`ring-supply.ts` 的 `why:'due'`），**不是** `ringCandidates` 环序路径。

**⇒ 修正后的准确图景（两条供给路径）**：
| 路径 | 覆盖 | 本轮实测产出 |
|---|---|---|
| **环序路径**（`ringCandidates`） | `ringOrder` **五环**，但需**情境键命中**（`cueDims` 仅 `scope`+`task`） | **0 行**（零命中） |
| **到期路径**（`dueSoon`，7 天窗） | **只有承诺** | **12 行** |

⇒ 即：**注入面看到的环全来自"到期提醒"这条窄路，环序路径零产出。**
**修法因此分两种，不可混淆**：① 环序路径是"未命中情境键"→ 补 cue 产出点；② 环序本身不含某环 → 改 `ringOrder`。
⚠ 本轮**尚未分清**（记入 U8 后半条）。

### F10.7 对方案的影响（**收益变大而非变小**）
不是"从零建通路"，而是"**已有通路的覆盖面只覆盖 1/7 环**" ⇒ 修法可能是**扩 `ring-supply` 的环序/额度**
（`DEFAULT_RING_ORDER` / `DEFAULT_RING_TOPN` / `adaptiveTopN` 三个可调点），**比原判更省**。
⚠ 但**为何只有承诺**这一点我**未拆解**（记入未验证项 U8）。

---

## 附：待其他席位复核的项（我未验）

| # | 项 | 状态 |
|---|---|---|
| U1 | `obs` 席 B1 的机理细节（写侧取 `yield-rounds` 末行 sid 的具体代码路径） | 我验了**现象**（时间不重叠），未逐行核机理 |
| U2 | `assumption` 席「`Get-Content` 丢 151 行」 | **我对拍否证**：`src/scheduler.ts` 三法均 801 行（`Get-Content -Encoding UTF8` / `NET ReadAllLines` ×2），raw LF=801、CR=0。⇒ 其主张在我这里**不成立**。但 `Measure-Object -Line` **确实丢行**（801 vs 1018），此为真 |
| U3 | `baseline` 席「LoCoMo 榜单饱和」「MIRIX 六类全部可注入」 | 外部检索项，我未独立复跑，采信其 URL 口径 |
| U4 | `arch` 席结构成因清单 | 该席 R1 尚未回报 |

---

_建立 2026-09-24 · 主持人自跑取证 · 每条附可复跑命令 · 含本人两处失误_
