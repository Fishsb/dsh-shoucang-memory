# 会话压缩记录 · 2026-09-21（评估通道收官 + 认知环 M3/P2/P3 + 判据面修复 + 治理收敛）

> **用途**：本件是一段**长会话的压缩重述**，遵循「**派生可回源**」——每条结论都留**可解析回源路径**
> （提交 sha / 判据件路径 / 台账字段），**不给断链的转述**。现场细节在提交与 `docs/OPEN-ITEMS.md` 原处，本件不取代它们。
> **一句话现状**：**工程面已闭环（可交钥匙）**；方案面剩 **2 项等触发**、**4 项等拍板**、**1 个板块未成档**；另有 **3 件我欠用户**的活。

---

## 1. 本会话落地的提交链（按时间序，全部已推送）

| 提交 | 内容 | 回源判据 |
|---|---|---|
| `9e93f77` | 记忆库根与 skill 装载面解耦（库根迁至 `<DSH_HOME>/suite/memory`） | 本会话起点 |
| `25fe40c` | 评估通道 M1/M2/M4 落地 + 面板化 + 模型选择器共用（84 件） | `/eval/config` 200 · 面板 6 控件渲染级实证 |
| `9c13053` | 方案档 §10 施工进度总表 + E-05 真机取证 | §10 |
| `c026391` | **E-05 幂等闸竞态修复**（推翻「真机未撞上」旧记） | `test-mcl-race` 9 条**先红→后绿** |
| `6660a76` | §0r 影子库对账红（**含两版错根因，后被撤回**） | §0r |
| `9ebdf16` | §0r 收口：镜像失败留痕 + 存量对齐 | `check-record-parity` 转绿 |
| `2acfcc4` | M3「频率分离」方案⟷验收成对（§11/§12） | §11/§12 |
| `a8f61ce` | **M3 落地**（M3a 判定/注入解耦 + M3b 环内换向出口） | `test-mcl` 54→**56 PASS** |
| `2df8ca6` | B1 改**请求计数实证** + 订正 §0s 过度怀疑 | 计数服务器 2 次 vs 0 次 |
| `e61f81e` | §0s 登记（重载报 OK ≠ 新码在跑）+ 层③判据缺口 | §0s |
| `24162aa` | §0s **闭环**（真机 7 条 judge 行 / 零 sim） | 台账 + `trace: pre` |
| `23430b3` | §0t 阈值校准**读数不稳定**（同 T 漂 5.8pp）+ 修 nav STALE | `mcl-calibrate` |
| `9d164ea` | **§13 继续推进方案**（P1–P6 跨板块视图） | §13 |
| `d264bc1` | **P2 先判后 cap 守卫** + **P3 对拍门归一化假红** + **P4 校准器稳定性自证** | `test-mcl-race` 13 · `inject-baseline-diff` 3 case 绿 |
| `d5d6dc7` | **P5 §0p 修门**（三类声明态中和）+ 6 项断链显式登记 | 自证 5 例含 3 反例 |
| `f6e80f7` | §13-P1 收口（真机慢通道三判据） | `mcl-switch` 4 条 · `echo` 2:36 |
| `39def09` | §13.7 P6 收口（归档 12 笔 / STALE 0 / id 二义定性） | `nav_graph mode=health` |

**五层终态**：`local == remote == 39def09` · 零未提交 · **pin 同指** · 副本 **314/314 sha1** · 热重载 active · **118 项标记** ·
套件 **197 pass / 0 skip / 1 fail**（唯一红＝既有 `eval-gate` 活模型非确定性）。

---

## 2. 本会话修掉的**真缺陷**（都是"机制在撒谎"型）

| # | 缺陷 | 形态 | 判据 |
|---|---|---|---|
| 1 | **E-05 幂等闸竞态** | `decideTurn` 的 `if (st.channel) return none` 与赋值之间隔**两个 await** ⇒ 两条入口同时越过 | 真机 **1859 组 / 3997 次多跑**；`st.deciding` 同址检查+置位 |
| 2 | **「先判后 cap」顺序性重复** | 本机宿主 `user/message` 落在当轮 pre-step **之后** ⇒ `captureTaskText` 清空 `channel` ⇒ 早判入口**再判一次**（**非并发** ⇒ E-05 的闸对它无效） | `shouldResetTurn` + `cap-dup` trace；**A3 前向判据：切点后重复对 0** |
| 3 | **影子库失步不可观测** | 整趟镜像 `catch{空吞}` **且丢弃返回值** ⇒ 失败无痕，唯一症状是事后对账红 | `mirrorFailuresOf` + 留痕；`record-sync --import` 对齐 |
| 4 | **`check-l0-conflict-wiring` 自身假绿** | `produces()` 的正则命中**类型声明行**与 **`l0Pick` 常量表** ⇒「声明了但无人产生」照样判绿 | 修后门**如实报出四维度 6 个取值零产生路径** + 自证 3 反例 |
| 5 | **对拍门假红**（`inject-baseline-diff`） | 成长横幅**两个数字**未归一 ⇒ 首差落在**字符 34**（活数据当骨架） | 补归一化 ⇒ **3 case 直接绿（未重冻结基线）** |
| 6 | **`test-prev-text-source` 锚点缺陷** | 用 `indexOf('prevText')` 开窗 ⇒ 被新函数**形参名**命中 ⇒ 假红 | 锚改 `let prevText` |
| 7 | **校准器读数不稳定** | 同一 `T=0.58`：4217 样本 27.7% → 5916 样本 **33.5%**；最近 1500 条 **58.3%** | 增**稳定性自证**（四子样本 + 带宽 31.8pp ≫ 距目标 0.6pp） |
| 8 | **网络：git 绕过系统代理** | WinINET 代理已开（`127.0.0.1:10808`，v2rayN+xray），而 `HTTP(S)_PROXY` 与 git `http.proxy` **全空** ⇒ 直连 github 被 reset | 本仓 `http.proxy=socks5h://…10808` ⇒ 推送一次即通 |

---

## 3. 本会话**我自己犯过并已订正**的四处（记档，防再犯）

1. **§0r 两版根因都错**（①"notes 零镜像" ②"ring-commit 陈旧快照"）—— 共同错因：**读码不完整就下结论**。
2. **§0q 判据无区分力**（用"同 `(sid,step)` 多行"，而 `step` **按轮重置**）⇒ 改用**时间间隔**才分离出信号。
3. **探针假绿**（`m3-live-measure` 在 N=0 时判据平凡成立仍打 ✅）⇒ 改为**零样本显式判"未取证"**。
4. **§0s 过度怀疑**（据零样本怀疑"重载静默停摆"，而 `mcl.ts:224` 的 **6 字门槛**是更简约解释）。
5. **`test-inject-baseline-normalize` A8 断言平凡**（用了夹具里不存在的行）⇒ 补夹具后才成真断言。

> **共同失败模式**：**先有立场再看数据**。据此在本会话内落成的约束：**判据必须先能证伪平凡解释**；**N=0 是未取证、不是证据**。

---

## 4. 开口项（三类 · 逐条附触发判据与复验命令）

### 4.1 等触发（无代码动作，只差数据/时间）

| 项 | 触发判据 | **实测现值** | 复验 |
|---|---|---|---|
| §12-B1 `S-P1b′`（痕迹文件数轴校准） | 新深睡纪元 **≥10** | **4 个** ⇒ 未到 | `_memory/audit/b1-tracefiles.mjs` |
| §12-B2 `J3/U1` 归因一致率 | `attributionSamples ≥ 30` | **52 行 · max=30 ⇒ 已到阈** | 同上 |
| §12-B6 `J5/U3` | 须先定"重定义 or 退役" | `switchSource` **95.7% 恒真**（7002 行）⇒ 判据仍无区分力 | `_memory/audit/closure-probe.mjs` |
| §12-B3 `S-P5c` 释放接线 | Jaccard ≥0.8（现 0.088） | 收敛手段在判据/提示层 | `scripts/essence-review-stability.mjs` |
| §12-B5 `H-10` | 自然跑 2–3 天 | 时间 | — |

### 4.2 等拍板（agent 不得自行推进 · R1/R3）

| # | 事项 | 现值 → 选项 | 我的判断 |
|---|---|---|---|
| 1 | 熟悉度阈值 | `0.58` → 保持 / `0.60` | **保持**（带宽 32pp ≫ 距目标 6.6pp）· 回滚 `/set mclFamiliarThreshold 0.58` |
| 2 | `coexist` 语义缺口 | 四维度 **6 值**零产生路径 → 补产生路径 / 收窄声明 | 属 **L0 语义变更**，须拍板 |
| 3 | `J5/U3` | 重定义 / **退役** | **退役更诚实**（恒真信号＝看起来在测实际测不出） |
| 4 | `evalEnabled` 缺省 | `false`（fail-closed）→ 保持 / 打开 | **保持关** |

### 4.3 未成档（缺前提）

- **「槽位预算接 UI」三册**：`docs/` 与 `skill/docs/` 内**查无对应方案档** ⇒ 只是承诺环里的一句待办 ⇒ **须先出"册一/册二/册三"的方案⟷验收成对档**。

### 4.4 我欠用户的（下一轮可直接做）

1. **TF-IDF+LR 基线 vs laya 中文 0.630 对比出数**（长期挂着，未做）。
2. **一份可直接落地的分步实施方案 + 分步验收方案**（每步：交付物 / 判据 / 回滚）—— §13 是跨板块视图，但**"通用模板级"那份未单独成档**。
3. 重启后验证 `shoucang-skill-filesystem` 已挂载、删旧址、复验技能仍在。

---

## 5. 文档面**已知的假绿风险**（下轮应先做）

**`docs/evaluation-channel-plan.md` 里并存多套新旧表格**：§4 分步 / §5 验收 / §7 档位 仍是 **v2 决策态原文**
（其中写着"待建""待授权"），而 **§10/§13 才是现态** ⇒ **光读文档会得出错误结论**（本会话即靠跑机检才发现）。
⇒ 建议：**把 §1–§9 显式降级为"历史决策态原文"，并合并出单一现态表**。

---

## 6. 回源索引（去哪读什么）

| 想查什么 | 去哪 |
|---|---|
| 分步状态 / 验收 A·B 组 / 继续推进 P1–P6 | `docs/evaluation-channel-plan.md` §10 / §11 / §12 / §13 |
| 各条缺陷的登记与收口（含我撤回的错误结论） | `docs/OPEN-ITEMS.md` §0n/§0o/§0p/§0q/§0r/§0s/§0t |
| 开口项汇总（待拍板 / 等触发 / 长期欠账） | `docs/OPEN-ITEMS.md` §12 |
| 真机取数探针（只读） | `_memory/audit/`：`m3-live-measure` · `echo-samples` · `a3-forward` · `b3-boundary` · `b1-tracefiles` · `closure-probe` · `e05-race-probe{,2,3,4,5}` · `parity-diff{,2}` |
| 判据 | `test-mcl` · `test-mcl-race` · `check-l0-conflict-wiring`(±`--selftest`) · `test-inject-baseline-normalize` · `inject-baseline-diff` · `mcl-calibrate` · `verify-open-items` |

---

_建档 2026-09-21 · 本件是**压缩重述**，判据与证据仍在原处（见 §6 回源索引）。_
