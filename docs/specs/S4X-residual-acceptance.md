# S4X · 遗留收尾验收方案（2026-09-14）

> 配套方案：`S4X-residual-plan.md`。**先出验收，再施工。**
> 命令一律在仓根执行；`npm run build:host` 是测试件前置；**改 `src` 后必须 `deploy-installed` + 热重载**才验运行态
> （S4R/R1 的教训：**部署 ≠ 运行态生效**）。

---

## X1 · 修 L8（`droppedStable` 少算）

| # | 断言 | 判据 |
|---|---|---|
| **A1** | `droppedStable` **不再是硬编码 0** | 源码级：`panel-shared` 中该变量由真实丢弃数派生（`dA.length + dU.length` 或 `cache.realCut.dropped` 同源） |
| **A2** | **差异只许出现在末尾预算提示行** | 用 `inject-baseline-diff` 对比 ⇒ **必然 FAIL**（文本有意变化），但**首个差异位置必须落在**「（本步受预算 … 省略 N 行…）」该行内；**若落在别处 ⇒ 判 FAIL 并回退**。须贴出差异两侧上下文作证 |
| **A3** | **先红**：修复前能实证"少算" | 用 `cache.realCut.dropped.length + droppedOneshot.length` 与提示行里的 N 对账 ⇒ 修复前 **N < 真实**（差 = `dA + dU`）。**首跑必须能判定**（无法判定 ⇒ 夹具不成立，须换更紧的预算） |
| **A4** | 修复后**数字守恒** | `droppedAll == dA.length + dU.length + droppedOneshot.length`；且提示行的 N == `droppedAll` |
| **A5** | 基线已更新且**变化被记录** | `inject-baseline-diff --write` 已执行；CHANGELOG 记一行（**用户可见**）；记录里写明"有意变化 + 正反向证据" |
| **A6** | 门禁与运行态 | `check-runner` 全绿 + `check-module-growth` PASS（`panel-shared` ≤790）+ `check-installed-sync --strict` 0 漂移 |

## X2 · L2 长函数（**决策卡**）

| # | 断言 | 判据 |
|---|---|---|
| **B1** | 给出**实测行数**与判定口径 | `node scripts/audit-fnspan.mjs` 输出中两函数的行数；并写明 `audit-fnspan` 是**报告态**（不判失败） |
| **B2** | 给出**决策 + 理由** | 决策 ∈ {拆, 不拆}；理由须含：**是否为领域接缝**（若只是按行数切 ⇒ 门禁明令禁止）、**是否改动行为**、**承接门禁**（I1 是否判它） |
| **B3** | 若"拆" ⇒ **行为不变**证据 | 对拍（`inject-baseline-diff`）或对应单测；若"不拆" ⇒ 理由须**可检验**（不是"收益低"这种空话） |

> **"不拆"是合法结论** —— 只要理由成立（例如：两函数非装配函数、`audit-fnspan` 不判失败、
> 现有接缝已在领域层划过，再切即"按行数硬切"）。本卡**不预设必须拆**。

## X3 · 待条件项复验入口汇总

| # | 断言 | 判据 |
|---|---|---|
| **C1** | `S4R-reverify.md` 覆盖**全部**待条件项 | 逐项存在：**L1**（深睡跨日二次激活）· **L3**（`forget` 候选）· **A4**（环记录可达度）· **D4**（零贡献率）· **D2**（两条件分流）。每项含**命令 + 基线值 + 判读标准** |
| **C2** | 与 MASTER §7 **逐条对齐、无遗漏** | 对账：§7 中「待条件」类项在本手册**都有**入口；反之手册不含已完成项 |

## X4 · 文档与仓库同步

| # | 断言 | 判据 |
|---|---|---|
| **D1** | MASTER §7 已更新 | L4 标"已撤销"（并注明理由：R1 证明无须决策）· L5/L6 标已完成 · L7 标"判据已重定" · **L8 已收录** · 指向 S4X |
| **D2** | INDEX 有 S4X 引用 | `INDEX.md` §7 下新增 S4X 小节（两份文档） |
| **D3** | 非本仓产物**只标注、不处置** | `deliverables/arch-overview-*`：**仍存在**（`Test-Path` 为真）· **未加入 `gitignore`**（`git check-ignore` 不命中）· 归属在记录里写明"非本会话产出，未纳入提交，待用户判断" |

---

## 判定口径

- **X1 / X2 / X4 全绿**；**X3 为文档类**（C1/C2 全绿）。
- **X2 允许"结论是不拆"** —— 验收判的是**决策有据**，不是"必须动代码"。
- **X1 的"对拍 FAIL"不算未通过**：本卡**预期的行为变更**，判据是**差异位置受限**（A2）+ **数字守恒**（A4）。
  ⇒ 这正是"**有意变更**"与"**回归**"的区分方式：前者差异**可逐字符解释且位置受限**，后者不然。
- 任一断言 FAIL ⇒ 该卡未完成，不进入下一卡。

---

_建立 2026-09-14 · S4X 验收方案 v1（先验收后施工）。_
