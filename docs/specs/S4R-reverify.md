# S4R · 待数据项复验手册（2026-09-14）

> **本手册是 S4 / S4R 中「待数据」项的唯一复验入口** —— 数据到位后照此执行即可判定各项。
> **为什么单独成文档**：这些项的判据**依赖样本**，而样本为 0 时**不得猜**（验收方案 C3）。
> 把复验方法固化下来，才能保证"将来有数据时不会用错判据"。

---

## 0 · 三分钟看全貌（一条命令）

```bash
node scripts/recall-diagnose.mjs
```

输出：归因分布 + 🧭 **由数据推导**的调参建议（非写死）。

---

## 1 · D2 两条件分流（**判据已重定**，待样本）

| 项 | 内容 |
|---|---|
| **字段** | `missReason`（`recall-empty` / `no-highconf` / `below-threshold` / `embed-off` / `ok`），**2026-09-14 加入** |
| **命令** | `node scripts/recall-diagnose.mjs` |
| **门槛** | 可归因 **≥ 30** 条（判据下限；低于此值输出 `insufficient-data`） |
| **判读** | 看 `advice`：<br>· `fix-recall` ⇒ **不要调阈值**，应查词表/索引/情境<br>· `lower-threshold` ⇒ 才可调 `mclFamiliarThreshold`<br>· `enable-embed` ⇒ 先启用向量 |
| **复验前基线** | 可归因 **0** 条 · 库内可召回索引行 **106** · mcl-step 总 **1437**（冻结 936 + 现行 501） |
| **⚠ 纪律** | **判据不可写死** —— 原写死判据的前提「瓶颈在标签门」**已被 S1 证伪** |

## 2 · A4 环记录可达度

| 项 | 内容 |
|---|---|
| **基线** | 环记录 **105 条**（全部 `file=''` 无 md 投影）· 情境槽 `topN` = **11**（S4-5：由固定 3 改为 `clamp(ceil(√N),3,12)`） |
| **命令** | `node scripts/recall-diagnose.mjs`（看"库内可召回索引行"）<br>`curl "http://127.0.0.1:3080/api/shoucang-panel/inject/preview?q=<情境线索>"`（看情境块） |
| **判读** | 情境块行数 **> 0** 且 ≤ `topN`；为空 ⇒ 线索未命中（**属正常**，非故障） |
| **⚠ 注意** | 情境槽要求 `cues.length > 0` ⇒ **无线索时不供给是设计使然**，不是"可达度差" |

## 3 · D4 零贡献率改善

| 项 | 内容 |
|---|---|
| **基线** | 零贡献率 **49.3%**（S1 实测） |
| **命令** | `node scripts/mcl-calibrate.mjs`（阈值 × 标签门矩阵，**双源读**） |
| **判读** | 零贡献率**下降** = 改善；同时看"有候选轮次落地率"（S3 基线 **71%**）——**低不等于空转** |
| **方向** | 下降为改善 |

---

## 3.1 · L1 深睡「跨日二次激活」

| 项 | 内容 |
|---|---|
| **基线** | **未观测到**（需**真实深睡**发生；输入侧已就绪：`replayHits` 实测 **10 条**） |
| **前置** | 需**全会话停滞 ≥3h**（本会话活跃 ⇒ 不可制造；不能靠改代码"造"出来） |
| **命令** | `node scripts/recall-diagnose.mjs`（看 `replay` 候选）<br>`node scripts/sleep-selfcheck.mjs`（深睡自检）<br>或查审计：`knowledge/audit/ledger.jsonl` 中 `type=deep-sleep` 行的 `replayHits` |
| **判读** | `replayHits` 非零 = 有跨日再现候选；**≥1 条原则被跨日二次激活**（即该节在两轮深睡中都被消费）才算达成 |
| **方向** | 出现即达成（当前 `forget`/`replay` 均可能为 0 ⇒ **空是正常**，非故障） |

## 3.2 · L3 `forget` 候选消费

| 项 | 内容 |
|---|---|
| **基线** | 主库 **2026-09-13 才重建** ⇒ 90 天窗口内**不可能**存在"≥90 天零命中"的冷节 ⇒ `forget` 输入恒 **0** |
| **命令** | `node scripts/recall-diagnose.mjs` 或直接读 `deep-sleep` 审计行的 `forgetCandidates` |
| **判读** | `forgetCandidates > 0` ⇒ 应看到 `forgetArchived`/`forgetKept` 有消费；`= 0` ⇒ **正确期望是输入 0**（不得硬消费 —— 那叫造数据） |
| **方向** | **待库龄**：约 **2026-12** 之后才可能有冷节 |

---

## 4 · 复验记录（每次复验后追加一行）

| 日期 | 项 | 样本量 | 结论 | 依据 |
|---|---|---|---|---|
| 2026-09-14 | D2 | 0 | `insufficient-data`（**不猜方向**） | `recall-diagnose` |
| 2026-09-14 | A4 / D4 | —— | 未复验（依赖 D2 样本先到位） | —— |

---

_建立 2026-09-14 · S4R/R4 交付物。_
