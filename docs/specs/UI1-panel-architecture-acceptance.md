# UI1 · 面板架构整理 —— 验收方案（2026-09-15）

> 配套：`UI1-panel-architecture-plan.md` · 判因：`UI1-adversarial-review.md`
>
> **两条总纪律**：
> ① **全绿方进** —— 任一断言 FAIL ⇒ 该卡未完成，不得进入下一卡（沿用五阶段口径）。
> ② **先红后绿**（S4R 立）—— 凡断言"等价 / 不变 / 已对齐 / 已生效"，**首跑必须 FAIL**；
>    不红即说明该断言**测的不是它声称测的东西**，须改写断言而非庆祝通过。
>
> **统计口径纪律**：本轮所有行数/符号数**必须用 node 解析**产出。PowerShell 对 `body.js`
> 给出过 5117（偏差 360）与两次 0 的假值 —— **基线口径错，门就是错的**。

---

## U0 · 让纪律可见

| # | 断言 | 先红验证 | 判据 |
|---|---|---|---|
| **U0-a-1** | `check-module-growth.mjs` **支持多根 + `.js`** | —— | 输出里出现 `body.js`（原实现硬编码 `join(root,'src')` + `**/*.ts`） |
| **U0-a-2** | **先红**：改门后首跑报 `body.js` 超基线 | ✅ **必须 FAIL** | 不红 ⇒ 改动没生效 ⇒ **断言无效，须排查** |
| **U0-a-3** | 冻结基线 = **node 解析的 5477** | —— | 契约里写明**解析器与命令**（可复现） |
| **U0-a-4** | 棘轮**只许收紧** | —— | 后续每卡下调，**不得上调** |
| **U0-b-1** | `check-ui-components.mjs` 存在且登记进 `CHECKS` | —— | runner 可见 |
| **U0-b-2** | **字面量对账**：`vendor.js` import ↔ `wa-*` 字面量，**两个方向都报** | —— | import 了不用 / 用了没 import |
| **U0-b-3** | **门内明写局限** | —— | 源码头注声明"运行时生成（`UI.button` 等）**扫不到**"，**不得声称覆盖全部** |
| **U0-b-4** | **反例自证** ×2（两个方向各一） | —— | 注入「import 不用」⇒ 检出；注入「用未 import」⇒ 检出 |
| **U0-c-1** | `src-client/` 解析可读性断言 | —— | 语法错误 ⇒ FAIL（当前无任何基线） |

## U1 · 打破闭包（**U2 的前置**）

| # | 断言 | 判据 |
|---|---|---|
| **U1-a-1** | **先红**：`ctx.js` 建成**前**，断言"顶层共享符号 = 0"**必须 FAIL** | 实测 **98** ⇒ 不红则断言无效 |
| **U1-a-2** | `UI.*` 封装**只在 `ui-kit.js` 定义** | `body.js`/`panes/*` 无 `UI.xxx = …` 定义（现为 `body.js` L1334 `var UI = {`） |
| **U1-a-3** | **不造全局** | 不出现 `window.xxx =`／隐式全局（`ctx` 以参数传递） |
| **U1-a-4** | **行为不变** | `ui-geo-regress` **100 PASS** + **U5 行为证据** |

## U2 · 逐 `render*` 迁移（**31 个**）

| # | 断言 | 判据 |
|---|---|---|
| **U2-1** | 迁移**逐个进行**，一次一个 `render*` | 每卡在提交信息里写明迁了哪个 |
| **U2-2** | **零逻辑改动**（唯一允许的改造 = 打破闭包） | 每卡跑 `ui-geo-regress` 100 PASS **且** U5 全绿 |
| **U2-3** | `body.js` 行数**逐卡下降**（node 计量） | 每卡记录；**棘轮同步下调** |
| **U2-4** | **契约不变** | `check-panel-contract` + `gen-panel-contract --check` 三向一致 |
| **U2-5** | 无新增循环 | `audit-architecture` 静态循环 **0**（含前端模块） |
| **U2-6** | **不搬家式膨胀** | 新模块行数之和 ≤ 原 `body.js` +5%（对抗估：import/export 样板约 **1–2%**） |
| **U2-7** | 全部 31 个 `render*` 迁完 | 终态 `body.js` **< 800** |

## U3 · 核对既有判因（**不动实现**）

| # | 断言 | 判据 |
|---|---|---|
| **U3-1** | `wa-select` 判因**核对结论写入文档** | 结论明确（仍有效 / 已失效），**不擅改实现** |
| **U3-2** | 判因从注释**提升为可机检说明** | 读码者不跑 `ui-geo-regress` 也能看到 |
| **U3-3** | **零视觉回归** | `ui-geo-regress` 100 PASS + 两套皮肤（`sc-skin-v9`/`sc-skin-host`）切换正常 |
| **U3-4** | **不得**与 `ui-geo-regress` 现有断言冲突 | 「零 `wa-select` 残留」若被打破 ⇒ 立即 FAIL |

## U5 · 行为级证据（补 §1.6 缺口）

| # | 断言 | 判据 |
|---|---|---|
| **U5-a** | **`/save` 端到端**正常路径 | 请求 → **200** → **数据回流且 DOM 更新** |
| **U5-b** | 拆分前后**同一次提交的 DOM 差异为空** | 施工**前**先落基线，每卡比对 |
| **U5-c** | **反例自证**：故意断一条回流 ⇒ U5 红 | 断言语义有效 |

---

## 总验收（全部卡完成后）

| # | 断言 |
|---|---|
| **Z1** | `npm run typecheck` + `npm run build`（host **+ client**）零错 |
| **Z2** | `node scripts/check-runner.mjs` 全绿（含 U0-b / U0-c 两个新门） |
| **Z3** | `ui-geo-regress` **100 PASS** |
| **Z4** | `check-panel-contract` + `gen-panel-contract --check` 三向一致 |
| **Z5** | `check-module-growth`：**`src-client/` 在扫描面内**且全绿（棘轮已调到实际值） |
| **Z6** | 运行态：`deploy-installed` → `check-installed-sync --strict` → **热重载** → `check-installed-features` |
| **Z7** | `check-public-tree` PASS（推送前） |
| **Z8** | CHANGELOG 记行；`client.js` 体积无异常膨胀 |

## 量与账（**施工前已实测**，终态核对）

| 指标 | 施工前（node 解析） | 目标 |
|---|---|---|
| `body.js` 行数 | **5477** | **< 800** |
| 函数定义 | **210** | 31 个 `render*` 全部迁出 |
| **顶层共享符号** | **98** | **0**（全在 `ctx.js`） |
| `render*` 迁移进度 | 0 / 31 | **31 / 31** |
| `src-client/` 模块数 | 4 | **≥ 14** |
| `src-client/` 门禁数 | **0** | **≥ 3** |
| 行为证据覆盖 | 几何 + 组件承载 | **+ `/save` 端到端** |

---

_建立 2026-09-15 · 与 `UI1-panel-architecture-plan.md` 成对（**验收先出**）。_
