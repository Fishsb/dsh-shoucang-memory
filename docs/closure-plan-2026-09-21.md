# 全仓收口方案（2026-09-21）

> **用途**：一份**可执行的收口方案** —— 回答"还剩什么、卡在哪、下一步做什么、谁授权、怎么回滚"。
> **口径（本档独有）**：表中**每个「现值」都是本会话实测**（附复验命令）；**未取证的显式写"未取证"**，不给推断值。
> **与既有档的关系**：本档是**跨板块的收口视图**；分步状态在 `docs/evaluation-channel-plan.md §10/§11/§12/§13`，
> 逐条登记在 `docs/OPEN-ITEMS.md`（§0n–§0t + §12），会话压缩重述在 `docs/handover-2026-09-21.md`。本档**不取代**它们。

---

## 1. 现态快照（实测 · 2026-09-21）

| 面 | 值 | 复验 |
|---|---|---|
| 仓库 | `local == remote == 5989f14` · 零未提交 · **pin 同指** | `git status -sb` · 核 profile `dependencies['dsh-shoucang-memory']` |
| 部署面 | 副本 **314/314 sha1 一致** · 热重载 active · **118 项标记** | `check-installed-sync --strict` · `check-installed-features` |
| 套件 | **197 pass / 0 skip / 1 fail**（唯一红＝既有 `eval-gate` 活模型非确定性） | `npm test` |
| 认知环真机 | `mcl-switch` **4 条** · `echo` **true 2 / false 36** · **A3 前向判据：切点后重复对 0** | `_memory/audit/{echo-samples,a3-forward}.mjs` |

**⇒ 工程面已闭环：可交钥匙。**

---

## 2. 开口项（四册 · 按"谁欠谁"分）

### 册一 · 等触发（**无代码动作**，只差数据/时间）

| # | 项 | 触发判据 | **实测现值** | 复验 |
|---|---|---|---|---|
| T1 | `S-P1b′` 痕迹文件数轴校准 | 新深睡纪元 **≥10** | **4 个** ⇒ 未到 | `_memory/audit/b1-tracefiles.mjs` |
| T2 | `S-P5c` 释放接线 | Jaccard ≥0.8 · 极差 ≤10pp | 6 真纪元通过率 **27.2 / 29.1 / 37.5 / 25 / 18.3 / 29.8%** ⇒ **极差 19.2pp · 未判率 29.1pp** ⇒ **未收敛** | `node scripts/essence-review-stability.mjs --from-ledger 6` |
| T3 | `H-10` S6 观测收尾 | 自然跑 **2–3 天** | 时间未到 | — |

### 册二 · ⚠ **仪表盘可疑**（**新发现** · 优先于一切"等触发"）

| # | 项 | 事实 | 判因（我的判断） |
|---|---|---|---|
| M1 | **`J3/U1` 归因一致率恒 0** | `attributionVerdict = **insufficient**`（note「未命中样本 N < 30」，各纪元 12–30）⇒ **触发未到**；而 `attributionAgrees` 在**累计 625 个有效样本里全为 0** ⇒ **一致率 0.0%** | **0/625 不像"真不一致"，更像"字段没被正确写"** —— 与本仓已知同型（`compliant=true` 恒 0 ⇒ 合规率 0.0%，"看起来在测量、实际测不出"）。⚠ **我上一条消息曾据 `attributionSamples=52` 说"已到阈"，那读的是触发器计数、不是判据面 ⇒ 已订正。** |
| M2 | `J5/U3` 换向信号恒真 | `switchSource` **6698/7002 = 95.7%** | 恒真信号＝无区分力 ⇒ **"重定义 or 退役"须先定案**，否则接判定也是接一个恒真量 |

> **M1/M2 同族**：都属「**看起来在测、实际测不出**」。⇒ 建议**先修仪表盘，再据此判任何收敛性**（否则 T2 的"未收敛"也可能是仪表盘问题）。

### 册三 · 等拍板（**agent 不得自行推进 · R1/R3**）

| # | 事项 | 现值 → 选项 | 我的判断 | 回滚 |
|---|---|---|---|---|
| D1 | 熟悉度阈值 `mcl.familiarThreshold` | `0.58` → 保持 / `0.60` | **保持**：同 T 在四条子样本上过线率 **33.5 / 26.5 / 40.4 / 58.3%** ⇒ **带宽 31.8pp**，而 0.58→0.60 只值 **6.6pp** ⇒ 结论在**噪声带** | `/set mclFamiliarThreshold 0.58`（热生效） |
| D2 | `coexist` 语义缺口 | 四维度 **6 个取值零产生路径** → 补产生路径 / 收窄声明 | 属 **L0 语义变更**，须拍板 | 已登记 `criteria.json#wiring.unreachableValues` |
| D3 | `J5/U3` | 重定义 / **退役** | **退役更诚实**（恒真信号留着就是第二条 M 类缺陷） | `check-claim-alignment` |
| D4 | `evalEnabled` 缺省 | `false`（fail-closed）→ 保持 / 打开 | **保持关** | 面板可开 |

### 册四 · 未成档（**缺前提，须先有成对档**）

| # | 项 | 事实 | 闭环前提 |
|---|---|---|---|
| N1 | **「槽位预算接 UI」三册** | `docs/` 与 `skill/docs/` 内**查无对应方案档** ⇒ 只是承诺环里的一句待办 | **先出「册一/册二/册三」的方案⟷验收成对档**（每册：交付物 / 判据 / 回滚） |

---

## 3. 建议执行顺序

| 步 | 做什么 | 判据（完成即勾） | 授权 |
|---|---|---|---|
| **S1** | **修仪表盘**：核 `attributionAgrees` 写入路径（为何 625 样本全 0） | 要么给出"真不一致"的证据，要么修好字段并留痕；`check-*` 相应门加判据 | ✅ 可自主（只读取证 + 如属缺陷则修，走五层） |
| **S2** | **N1 成对档**（槽位预算三册：册一/册二/册三） | 三册各含 交付物/判据/回滚 + **存量先对齐**（`[路径] 多链条改造分册`） | ✅ 可自主（出档属决策态） |
| **S3** | T1/T2 按触发条件复跑 | T1 纪元 ≥10；T2 极差 ≤10pp | ✅ 可自主（只读复跑） |
| **S4** | D1–D4 落值 | 各一条 | ❌ **须你一句话** |
| **S5** | 我欠你的 3 件（见 §4） | 各一条 | ✅ 可自主 |

**为什么 S1 排在 T2 前**：T2 报的是"未收敛"，但若 M1 的仪表盘本身不可信，T2 的读数**同样待证** ⇒ **先修仪表盘再下收敛性结论**（同 `[原则] 代理指标非判据`）。

---

## 4. 承诺对账

**我欠你的（3 件 · 可自主做）**
1. **TF-IDF+LR 基线 vs laya 中文 0.630 的对比出数**（长期挂着，未做）。
2. **通用「分步实施 + 分步验收」模板档**（每步：交付物 / 判据 / 回滚）—— §13 是跨板块视图，**模板级那份未单独成档**。
3. 重启后验证 `shoucang-skill-filesystem` 已挂载、删旧址、复验技能仍在。

**你欠我一句话的（4 件 · 即册三 D1–D4）**：阈值 0.58/0.60 · `coexist` 是否补语义 · `J5/U3` 重定义或退役 · `evalEnabled` 缺省。

---

## 5. 回源索引

| 想查 | 去哪 |
|---|---|
| 分步/验收/继续推进 | `docs/evaluation-channel-plan.md` §10 / §11 / §12 / §13（**§1–§9 为历史决策态，勿引**） |
| 逐条登记与收口（含撤回的错误结论） | `docs/OPEN-ITEMS.md` §0n–§0t · 开口项汇总 §12 |
| 本会话压缩重述 | `docs/handover-2026-09-21.md` |
| 真机取数探针（只读） | `_memory/audit/`：`closure-probe` · `j3u1-probe` · `j3u1-rate` · `b1-tracefiles` · `b3-boundary` · `a3-forward` · `echo-samples` · `m3-live-measure` |
| 判据 | `test-mcl` · `test-mcl-race` · `check-l0-conflict-wiring`(±`--selftest`) · `test-inject-baseline-normalize` · `inject-baseline-diff` · `mcl-calibrate` · `essence-review-stability --from-ledger` |

---

_建档 2026-09-21 · **本档只写实测值**；未取证处显式标注。_
