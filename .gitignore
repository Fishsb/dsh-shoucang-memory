# 治理文档本地化：不入开源发布仓（2026-09-08 pmg 治理体系已整体移除；本文件仍按开源红线本地化）
AGENTS.md
CLAUDE.md
HANDOVER.md
# 2026-09-17 加固（圆桌会审 I2）：根目录画像/索引三件**曾已入库、且不在任何隐私门覆盖面**
#   （git check-ignore 对三者均无命中；check-public-tree 的 FORBIDDEN 亦未列）。三件当前仅 3 行外壳、
#   无个人数据，但本仓根一旦被登记为守藏 root，画像/索引行就会被写进这里并**静默上公开仓**。
#   根部解决：gitignore 挡住 + FORBIDDEN 同步收录 + `git rm --cached`（三处同改，缺一处仍复发）。
/MEMORY.md
/USER.md
/AGENT.md

# 依赖与构建产物（注意：lib/ 构建产物按 dsh 插件仓惯例提交入库，不在此列）
node_modules/
dist/
.tsbuild/
__pycache__/

# 本地环境
.env
*.local
# 本机面板实例配置（登记本机工作区为守藏 root 时生成；含本机可调值，不入公开仓）
shoucang.config.yaml

# ---- 瞬态数据 ----
.internal/

# ---- 私人记忆数据区（2026-09-08 自 dsh-managing-memory 迁入；隐私红线：不入公开仓）----
/_memory/

# ---- 开发知识库（本机参考，不入公开仓）----
docs/devref/
skill/docs/devref/

# ---- 记忆脚本瞬态产物（沿自源仓 gitignore）----
.tmp-*
*.tmpdir/
*.tmp
# 一次性重构/迁移脚本（2026-09-12 清理）：阶段 C 的 8 件 `_tmp-*.mjs` 曾误入公开仓，
#   而 files 含 scripts ⇒ 被打进发行包。**根部解决**：gitignore 层挡住，新增同类不再可能被 add。
scripts/_tmp-*

# ---- WorkBuddy 本地工作记忆（AI 协作数据，不入公开仓）----
.workbuddy/
.workbuddy-ai/

# ---- 视觉工具链本机缓存（@anionex/dsh-vision-toolkit 的 artifacts/tmp；含本机会话图片副本，不入公开仓）----
.dsh-vision-toolkit/
# S3：client 打包中间产物（构建脚本失败时会留下，成功后自动清理）
client.js.build.tmp
# S3：组件库主题令牌层的展平产物（build:client 生成）
src-client/.vendor-css.generated.js

# ---- 重构前的本地备份（本地保险，**不入公开仓**）----
# 背景：2026-09-13 拆前端大函数时按纪律给 body.js / client.js 加了 `.bak-<阶段>-<时间戳>`，
#   但当时 .gitignore 无对应规则 ⇒ `git status` 里是 untracked、`git add .` 会直接带进公开树。
#   本仓有「推送前确认公开树纯净」红线，故在根部设防（与此前 `scripts/_tmp-*` 同一处理方式）。
*.bak-*
*.pre-u1

# 面板 /root/bootstrap 若被误指向仓库根，会生成单库骨架桩（2026-09-13 实测）
/whitelist.json

# 面板 /root/bootstrap 若被误指向仓库根，会生成单库骨架桩（2026-09-13 实测）
/memory-whitelist-spec.md

# 面板 /root/bootstrap 若被误指向仓库根，会生成单库骨架桩（2026-09-13 实测）
/task-protocols.md

# 面板 /root/bootstrap 若被误指向仓库根，会生成单库骨架桩（2026-09-13 实测）
/human-execution-loop.md

# 面板 /root/bootstrap 若被误指向仓库根，会生成单库骨架桩（2026-09-13 实测）
/notes/

# 过程性 UI 比对截图（319 件 / 27 MB，可重生成；2026-09-15 移出仓库，本地保留）
deliverables/v9-compare/

# 过程性 UI 出图（UI1 收尾 · 2026-09-15）：11 张 / 1.2 MB，可重生成
#   依据 docs/PUBLISH-POLICY.md「过程性截图已移出仓库」；且 check-public-tree **跳过 .png**
#   ⇒ 图片无法被隐私门扫到，入库风险高于收益。复核方法与逐图结论见
#   docs/specs/UI1-acceptance-record.md §3″（人工视觉复核）
deliverables/ui1-shots/

# .roundtable/ —— 圆桌会议插件运行时数据（会议记录里 kbPath 是用户开会时选的**本机绝对路径**）
#   实测 git ls-files .roundtable = 0（未跟踪）⇒ 永不入库；同批已补进本文件与 SKIP_DIRS（2026-09-16）
.roundtable/

# AgentTeams 团队状态 —— 建团队时把工作区**绝对路径**写进计划文本（如“只读全项目检查本机工作区”）。
#   2026-09-17 实测：建团队前 check-hardcode 全绿，建团队后立刻报 2 处 ⇒ 同批已补进 SKIP_DIRS，
#   否则「未被跟踪」只是巧合，git add -A 就会带进去（与上方 .roundtable/ 同一条判据）。
.agent-teams/

# ---- i18n 翻译流水线瞬态产物（2026-09-17）----
#   判因：这些中间产物（分片/合并/译文）与改动前源码备份未 ignore 时，
#   任何一次 git add -A 都会把它们带进公开树（备份目录更会让源码出现双份、审计误读）。
#   译文已固化为 src-client/i18n-dict-*.js ⇒ 中间产物可安全丢弃。
/.i18n-*.json
/.i18n-backup-*/
